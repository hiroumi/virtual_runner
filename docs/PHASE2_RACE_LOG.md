# Phase 2 作業記録（動く疑似3Dレーシングゲーム本体）

対象: Virtual Boy for Nintendo Switch アクセサリ向け立体視レーシングゲーム・プロトタイプ

前提: [[docs/PHASE2_STEREO_TEST_LOG.md]] の静的ステレオ確認シーンを実機で確認済み、との報告を受けて着手。

## 実装内容

### `road.py`（新規）

セグメント方式（Out Run系の古典的手法）による道路モデル。道路を一定長のセグメントの列として表現し、各セグメントが持つ`curve`値を2回積分（curve→方向→world_x）することで、実際の3D形状データなしに滑らかに曲がる道を表現している。

- `SEGMENT_LENGTH=3.0`、`screen_depth=20`など既存のdisparity設定と**同じワールド単位**を使うように意図的に設計。カメラからのセグメント距離をそのまま`calculate_disparity`のdepthとして使えるようにするため（新たな単位変換を導入していない）。
- 1コースはカーブ区間とストレート区間を組み合わせて約620セグメント（1860ワールド単位）。想定巡航速度（約30単位/秒）で60〜90秒のプレイ時間になるよう調整・確認済み（`tests/test_road.py`でも検証）。
- カーブの強さ（curve値）は、当初クラシックな実装例をそのまま流用したところ道路が完全に画面外へ飛び出す不具合が発生。ヘッドレス環境でのレンダリング確認を繰り返し、約1/50に調整することで自然な曲がり方になることを確認した。

### `stereo_renderer.py` への追加

`StereoRenderer.project_x(local_x, depth)` を追加。道路の1セグメントは手前側と奥側で距離（depth）が異なるため、`draw_world`（1回の呼び出しにつき単一depth前提）ではなく、4隅の頂点それぞれに個別のdepthを与えて視差計算できるようにした。これにより「道路セグメントごとに視差を計算する」というユーザー指示を、頂点単位の正しい実装として満たしている。

### `game.py`（新規、ゲーム本体）

- 後方視点、セグメント方式のスクロール道路
- アクセル（↑/W）、ブレーキ（↓/S）、左右操舵（←→ or A/D）
- カーブ（road.pyのcurve値がプレイヤーへの遠心力的な引っ張りとしても作用し、カーブでは無操作だとコースアウトする）
- プレイヤー車1台、敵車（一般車）複数台（固定シード配置、track上に間隔を空けて配置）
- 衝突判定：プレイヤーと敵車のワールドZ・X座標が近接すると減速（速度×0.5）＋1秒のクールダウン（連続ヒットで速度が0になり続ける事態を防止）
- コースオフ時は最高速度を制限（45→20）し摩擦も増加
- 1コース約60〜90秒（実測: 巡航速度で約62秒、加速し続ければもっと短い）
- HUD: TIME（カウントダウン、初期値90秒）、SCORE（速度×時間で加算）、SPEED
- ゴール到達で「FINISH!」、時間切れで「TIME UP」を表示し、Rキーでリスタート

### 視差関連（既存APIをそのまま再利用、変更なし）

- HUDは`draw_flat`で視差ゼロ
- 空・山は既存の静的テストシーンと同様、遠いdepth（250）で描画→小さな外向き視差
- 道路は`project_x`で頂点ごとに視差計算（手前ほど強い内向き、地平線ほどゼロに近い）
- 敵車・木・プレイヤー車は`draw_world`で単一depthの視差（プレイヤー車はPhase 2テストシーンと同じdepth=3を再利用）
- `parallax_scale`は実行中に`[`/`]`で調整可能（テストシーンではUp/Downだったが、本編ではUp/Downをアクセル/ブレーキに割り当てたため変更）。`Z`で全視差ゼロ化、`I`で視差方向反転のデバッグ機能も維持。
- `max_disparity_px` / `max_negative_disparity_px` による安全上限もそのまま有効（config.json変更なし）

### デバッグモード（Dキー）

「フレームレートと入力遅延を画面に表示できるデバッグモードを用意する」という元の指示に対応。実際の入力遅延の直接計測は困難なため、FPSとフレーム処理時間（simulation更新にかかった時間）を代理指標として表示している。

## 開発中に発見・修正したバグ

ヘッドレス環境でのスクリーンショット確認により、以下の不具合を実装中に発見し修正した（実機投入前に解消済み）：

1. **ゴール直前で道路が画面いっぱいの単色ブロックに崩れる不具合**: コース終端に近づくと、描画予定の全セグメントが「最後の実在セグメント」にクランプされ、実質同じ座標に大量の頂点が重なって縮退した多角形になっていた。セグメント配列の範囲外もworld_zを増加させ続ける（道路データの参照だけ最後のセグメントにクランプし、投影に使うworld_z自体はクランプしない）ように修正し、ゴール付近でも道路が自然に奥へ続いて見えるようにした。

## 動作確認（この環境内、実機なし）

- `pytest`: 36件全て成功（road.pyのトラック生成が妥当な長さ・カーブ範囲であること、ゲームのアクセル/コースト/コースオフ速度上限、衝突による減速とクールダウン、ゴール/タイムアップ遷移、リスタート、全キー操作、を含む）。
- ヘッドレスで実際に加速・操舵・衝突・ゴールをシミュレートしたスクリーンショットで、道路のカーブ、樹木、敵車、プレイヤー車、HUD、左右の視差差分を目視確認。

**実機での確認は未実施。** 次回、実際にアクセサリを覗きながらプレイし、以下を確認いただきたい：

1. `python main.py --race` をフルスクリーンで起動し、実際に走行できるか
2. カーブでの道路の曲がり方・操舵のフィーリングが破綻していないか
3. 敵車との衝突判定のタイミングが違和感ないか
4. `[`/`]`で好みのparallax_scaleを探し`S`で保存
5. ゴール・タイムアップ時のメッセージ表示、Rキーでのリスタートが機能するか

## 2026-09-02: カーブ形状の見直し（ユーザーフィードバック対応）

ユーザーより「カーブがきつい。曲がったらすぐ直線に戻ってほしい。S字や左右カーブ自体は良い」とのフィードバックを受け、`road.py`のカーブ生成方式を変更した。

### 原因

従来の実装（`_add_section`）は、カーブ区間で`curve`値をランプアップ→一定値で保持→ランプダウンさせていたが、**方向（direction、= curveの累積値）がカーブ区間終了後も元の値（0）に戻らない**設計だった。このため、カーブの直後に来る「直線」区間も、実際にはdirectionが非ゼロのまま一定なので、world_x（道路の横方向位置）がその区間中ずっと一定の割合で増え続け、画面上は「まっすぐな道」ではなく「一定角度で斜めに伸び続ける道」に見えてしまっていた。これがユーザーの言う「斜めの道路が続く」現象の原因。

### 修正内容

カーブ区間（`_add_bend`）を、curve値が **1周期分の正弦波（0→+peak→0→-peak→0）を描くように変更**。これにより、カーブ区間が終わった時点でdirectionが必ず元の値（0）に戻ることを保証した。結果として：

- カーブ区間：右（または左）に曲がり、区間内で自動的に反対方向へ戻って直進方向に復帰する、単発の「曲がってすぐ戻る」挙動になった。
- 直線区間：directionが常に0のまま安定しているため、見た目も完全にまっすぐな道として描画される。
- 長さを変えることで「素早いシケイン」（短いbend）と「ゆったりしたS字」（長いbend）の両方を表現できる。

修正後、direction値がカーブ終了直後に正確に0へ戻ること、直線区間中world_xが完全に一定であることを数値計算で確認済み。また実際に走行させたスクリーンショットで、カーブ直後の直線が正しくまっすぐ見えることを目視確認した（`pytest`36件も全て再度成功）。

## 2026-09-02: 高速コーナリングと3車線化（ユーザーフィードバック対応）

ユーザーより「カーブで大きく減速しないと曲がれない」「道路はアメリカ風に3車線くらいあるほうがいい」とのフィードバック。

### 高速コーナリング対応

原因: `CENTRIFUGAL`（カーブによる横方向への押し出し力）が`2.5`と大きすぎ、かつ`STEER_RATE`（操舵の効き）とのバランスが悪く、最高速付近でカーブに侵入すると大きく道路外へ押し出されていた。

対応: `CENTRIFUGAL`を`2.5→0.8`に低減し、`STEER_RATE`を`1.6→2.0`に引き上げた。目標値は「コース上で最もきついカーブでも、操舵なし・アクセル全開のままコースアウトしない」こととし、数式（横方向のずれ ≈ CENTRIFUGAL × カーブの強さ × カーブ通過時間 ÷ π）から逆算して係数を決定。実際に全区間を無操舵・アクセル全開でシミュレーションし、道路中心からの最大ズレが±1.0（コースアウトの閾値）に対して0.41にとどまり、最後まで完走できることを確認した。操舵は車線変更や敵車回避のために引き続き意味を持つ。

### 3車線化

`road.py`の`ROAD_WIDTH`を`9.0→11.0`に拡幅した上で、`game.py`の道路描画に破線の車線区分線を追加。ラインは道路を3等分する位置（中心から±1/3の位置）に配置し、既存のランブルストライプと同じ明暗の交互パターン（`RUMBLE_LENGTH`セグメントごと）を流用することで自然な破線表現にした。敵車の出現レーンも、この3等分の車線中央（-2/3, 0, +2/3）に揃うよう修正。

実装中、道路のごく手前（カメラ直下）のセグメントで投影スケールが極端に大きくなり、車線区分線だけ画面端に不自然な三角形状のノイズが出る問題が見つかったため、カメラからの最小距離のクランプ値を`0.1→SEGMENT_LENGTH(3.0)`に引き上げて解消した（幅の広い道路本体はこの値でも見た目に影響しないが、細い車線区分線ではこの近接特異点が目立っていた）。

修正後、`pytest`36件全て再度成功。ヘッドレスレンダリングで、直線・カーブ双方で3本の破線車線が地平線まで自然に収束して見えることを確認した。

## 2026-09-02: カーブをさらに緩和（ユーザーフィードバック対応）

ユーザーより「まだカーブがキツイ。大きなカーブはゆるく、長くしてスピードを保ったまま曲がれるように」とのフィードバック。

前回の対応（CENTRIFUGAL/STEER_RATEの調整）は「無操舵でもコースアウトしない」という物理的な余裕は確保していたが、カーブ自体の**曲率のピーク値が高いままだった**ため、見た目にも体感的にも「一瞬鋭く曲がる」印象が残っていたと考えられる。

対応: `road.py`のTRACK_EVENTSを全面的に見直し、大きなカーブほど**ピーク曲率を下げて区間を長くする**方針に変更した（例: 右カーブ 0.45/45segs → 0.18/80segs、左カーブ -0.40/50segs → -0.22/90segs、S字 0.35/70segs → 0.20/110segs）。終盤のシケイン1箇所のみ、変化をつけるためにあえて短く鋭いまま残した（0.32/40segs）。

結果、無操舵・アクセル全開で全区間を走行した場合の道路中心からの最大ズレは0.37（前回0.41よりさらに改善、コースアウト閾値1.0に対して大きな余裕）。コース全長は2490ワールド単位（従来比+約34%）で、最高速度を維持し続けた場合の理論上の最速タイムは約57秒（TIME HUDの初期値90秒に対して十分な余裕）。カーブの見た目も、遠方から緩やかに始まり長く続く形になったことをスクリーンショットで確認した。

## 2026-09-02: カーブ中の「曲がっている感」の追加（ユーザーフィードバック対応）

ユーザーより「カーブの時、車が奥にまっすぐ進んでいるのに道だけが斜めに出現しているように感じる。曲がっている方向へ右上/左上にドリフトしていく感じが欲しい」とのフィードバック。

### 原因

このプロジェクトが採用している疑似3D手法（セグメント方式、カメラのヨー回転なし）は、道路のworld_xをカメラ位置からの相対値として投影するだけで、**カメラ自体の向き（消失点の位置）はカーブ中も常に画面中央に固定**されたままになる。実際の運転では、カーブに入ると視界（背景・地平線）ごと回転して見えるはずだが、この実装では道路だけが横にずれて描画され、背景（山・雲）やプレイヤー車の画面上の位置は変化しないため、「道路だけが斜めに現れている」という違和感につながっていた。

### 対応

クラシックな疑似3Dレーシングゲーム（Jake Gordon氏のPseudo 3D Racerチュートリアル等）で使われる標準的な手法を追加実装した。

1. **背景（山・雲）のパン**: 現在のセグメントのcurve値と速度に応じて、背景全体を左右にパン（左右の目で共通、視差ではなく画面全体のシフト）させる。カーブが右（curve>0）のときは背景を左へ、左カーブのときは右へパンさせることで、「カメラ自体が旋回している」感覚を演出する。カーブが終われば自動的に中央（オフセット0）へ戻る（`bg_offset`を目標値へ指数的に近づける形で実装、急な切り替わりを防止）。
2. **プレイヤー車のリーン**: 同じcurve値・速度に応じて、プレイヤー車のスプライトをカーブが曲がっている方向へわずかに（最大11px）ずらす。背景が反対方向へパンするのと合わせて、「カーブの方向へ車がドリフトしていく」印象を強める。

いずれもゼロ視差の演出的な効果であり、既存のステレオ視差計算（`StereoRenderer`）とは独立している。

修正後、`pytest`36件全て成功。ヘッドレスレンダリングで、カーブ進入時に背景・プレイヤー車の位置が実際にシフトし、カーブが終わると中央へ戻ることを確認した。

## 2026-09-02: カーブ中のカクつき（ジャダー）修正

ユーザーより「まだカーブでカクついたりする。スムーズにカーブを曲がれるようにしたい」とのフィードバック。

### 原因

カメラの左右位置の基準点（`_road_center_x`、および敵車の描画位置）を、プレイヤーの現在地`player.z`が属する**セグメント単位の離散値**（`segment.world_x`）から求めていた。セグメント長は3ワールド単位で、カーブの強い区間ではセグメントごとにworld_xが最大で3.5ワールド単位近く変化する（道路の全幅11に対して約30%）。最高速（45ユニット/秒）で走ると1セグメントを通過するのに約66ミリ秒（60fpsで約4フレーム）しかかからず、その間はworld_xの参照値が変化しないまま、セグメントを跨いだ瞬間に一気に3.5ユニット近くジャンプする——という「4フレーム静止→1フレームで大きくジャンプ」を繰り返す形になっていた。これが体感的な「カクつき」の正体で、カーブでのみ顕著になるのは、直進区間ではセグメント間のworld_x差がゼロ（方向が変化しないため）で、この問題が起きないから。

同様に、現在のカーブ強さ（`segment.curve`、遠心力や背景パン・車のリーンの計算に使用）も同じ理由で段階的にしか変化しておらず、こちらもわずかなカクつきの一因になっていた。

### 対応

`road.py`に`world_x_at()`と`curve_at()`を追加し、隣接する2セグメントの値を`player.z`（あるいは敵車のz）の小数部分に応じて**線形補間**するようにした。セグメント境界ちょうどでは従来と同じ値になり、境界の間は滑らかに変化する。

- カメラの基準点（`_road_center_x`）: セグメント単位→補間値に変更
- 敵車の描画位置: セグメント単位→補間値に変更（プレイヤーと同様、常時移動するため）
- 遠心力・背景パン・車のリーンに使う現在のカーブ強さ（`current_curve`）: セグメント単位→補間値に変更
- 道路脇の樹木などの静止オブジェクトは、座標が毎フレーム変化しないため据え置き（補間の有無で見た目が変わらない）

修正の効果を検証するため、カーブの強い区間（セグメント390〜420付近）で、1フレーム分の細かい移動量ごとのworld_xの変化量を計算したところ、修正前は1回のセグンメント境界通過で最大約3.5の跳躍が起きていたのに対し、補間後は毎フレームなだらかに最大でも約0.9ずつ変化するようになり、跳躍が解消されたことを確認した。回帰防止のため`tests/test_road.py`にこの補間の滑らかさを検証するテストを追加（境界値の一致、細かいステップでの最大変化量の上限など）。

`pytest`40件全て成功。

## 2026-09-02: スピード感の向上（ユーザーフィードバック対応）

ユーザーより「かなり良くなった。もう少しスピード感を出したい。今は中くらいのスピードなので、さらに高速で走れる感じにしたい」とのフィードバック。

### 対応

`game.py`の最高速度`MAX_SPEED`を`45.0→60.0`（約33%増）に引き上げた。これに連動して、以下の値も同じ比率でスケールし、加減速の体感バランス（0→最高速の到達時間、ブレーキの効き、コースオフ時の速度低下）を維持した。

- `ACCEL`: 25.0 → 33.0
- `BRAKE`: 70.0 → 93.0
- `FRICTION`: 12.0 → 16.0
- `OFFROAD_FRICTION`: 45.0 → 60.0
- `OFFROAD_MAX_SPEED`: 20.0 → 27.0
- `TRAFFIC_SPEED`（敵車の速度）: 14.0 → 19.0（プレイヤーとの相対速度差を維持）

コース全長を維持したまま最高速度だけを上げるとコースが60秒未満で終わってしまう（60〜90秒プレイの仕様を割ってしまう）ため、`road.py`のTRACK_EVENTSの**直線区間のみ**を延長し、カーブ区間（ピーク値・長さ）は一切変更していない（カーブは別途スムーズさ・曲がりやすさをチューニング済みのため）。結果、コース全長は2490→3330ワールド単位、最高速度のみで走り続けた場合の理論的な最速タイムは約55.5秒で、実際に加速を含めてシミュレーションしたところ約57.8秒（変更前は約57.4秒）と、ほぼ同じペース感を保ったまま速度だけが約33%向上したことを確認した。

なお、`CENTRIFUGAL`（カーブでの遠心力）や`STEER_RATE`（操舵の効き）は`speed_frac`（現在速度÷最高速度、0〜1の正規化値）を基準に計算しているため変更不要だった。むしろ最高速度が上がったことで同じカーブを通過する実時間が短くなり、無操舵・アクセル全開でのカーブ通過時の最大ズレは0.28（変更前0.37〜0.41）とさらに安全になっていることを確認した。

HUD上のSPEED表示は、最高速度到達時240（変更前180）となり、見た目の数値としてもよりアーケードレーシングゲームらしい高速感が出ている。

`pytest`40件全て成功。ヘッドレスレンダリングでも、直線・カーブともに正常に描画されることを確認した。

## 2026-09-02: 最高速度320km/h化とカーブの再スケール（ユーザーフィードバック対応）

ユーザーより「まぁまぁ良くなったが、もっとスピード感を出したい。最高速320キロくらいにできるか。カーブは高速でも曲がれるよう、ゆるく長くすれば曲がりきれるのでは」とのフィードバック。

### 対応

HUDのSPEED表示は`player.speed * 4`で計算しているため、`MAX_SPEED`を`60.0→80.0`にすれば表示は`320`になる（320÷4=80）。これに合わせて、前回同様の比率（80/60 ≈ 1.333倍）で以下をスケールした。

- `ACCEL`: 33.0 → 44.0
- `BRAKE`: 93.0 → 124.0
- `FRICTION`: 16.0 → 21.0
- `OFFROAD_FRICTION`: 60.0 → 80.0
- `OFFROAD_MAX_SPEED`: 27.0 → 36.0
- `TRAFFIC_SPEED`: 19.0 → 25.0

### カーブの再スケール（ユーザーの提案どおり）

今回は、ユーザー自身の提案（「ゆるく、長く」）に従い、`road.py`の**カーブ区間の長さも同じ比率（1.333倍）でスケール**した（ピーク値=曲がりの強さは変更なし）。直線区間もこれまでと同様に延長している。

この「長さだけを速度と同じ比率でスケールする」方法には数学的な裏付けがある。1つのカーブを通過するのにかかる実時間 T は「カーブの長さ ÷ 現在の速度」で決まり、コーナリング時の横方向のズレの目安は「CENTRIFUGAL × カーブの強さ × T ÷ π」に比例する（過去の作業記録を参照）。速度と長さを同じ比率で増やすと T が変化しないため、**最高速度を上げても、カーブの安全マージンも体感的な「カーブの長さ（秒数）」もまったく変わらない**——つまり「高速化しても曲がりきれる」を厳密に成立させる形になっている。

### 検証結果

- コース全長: 3330 → 4455ワールド単位
- 最高速度のみでの理論最速タイム: 55.7秒（変更前55.5秒とほぼ同じ）
- 実際に加速を含めてシミュレーション: 58.45秒（変更前57.8秒とほぼ同じ）
- 無操舵・アクセル全開でのカーブ通過時の最大ズレ: 0.277（変更前0.276〜0.28とほぼ同一）→ 想定通り、高速化してもコーナリングの安全性・体感が変わらないことを確認
- HUDのSPEED表示: 最高速度到達時320

`tests/test_road.py`のコース所要時間チェックが、これまで固定値（30 units/秒）を基準にしていたため、`MAX_SPEED`の変更に追従できず一度失敗した。`game.MAX_SPEED`を直接参照する形に修正し、以後は最高速度をチューニングしても自動的に妥当性チェックが追従するようにした。

`pytest`40件全て成功。

## 2026-09-02: カーブをさらに2倍長く・緩く、体感速度を「実質360km/h」に

ユーザーより「だいぶ良くなった。カーブは今の倍くらい長く、その分ゆるくすることで高速カーブを走っている感じにしたい。最高速度は320キロでいいが、もう少し早めにしたい。360キロ相当を320キロと表示してもいいくらい」とのフィードバック。

### カーブを2倍長く・ピークを半分に

`road.py`の主要な曲がり区間（シケイン以外）について、**長さを約2倍・ピーク曲率を約半分**にした（例: 右カーブ 0.18/105segs → 0.09/210segs）。終盤のシケインのみ、あえて短く鋭い区間として残すため、長さだけ2倍にしピークは変更していない（0.32/55segs → 0.32/110segs）。

「長さ×ピーク曲率」の積を一定に保つ設計にしているため、遠心力による横方向のズレの安全マージンは変更前とほぼ同じに保たれる（過去の作業記録で導出した式: ズレ ≈ CENTRIFUGAL × ピーク曲率 × 通過時間 ÷ π。長さを2倍・ピークを半分にすると、通過時間は2倍になるがピークが半分になるため、積は変わらない）。

### 最高速度「表示は320のまま、中身は実質360相当」

HUDのSPEED表示を、これまでの`speed * 4`という固定倍率から、**「最高速度に達したとき常にHUD_MAX_DISPLAY_SPEED（320）と表示される」計算式**に変更した（`speed / MAX_SPEED * HUD_MAX_DISPLAY_SPEED`）。これにより、実際のゲーム内の最高速度（`MAX_SPEED`）を今後さらにチューニングしても、表示上の最高値は常に320に固定できるようになった。

今回はこの仕組みを使い、`MAX_SPEED`を`80.0→90.0`（表示上の320に対して、実質「360km/h相当」を意図した引き上げ）にし、加速・ブレーキ・摩擦・敵車速度も同じ比率（90/80=1.125倍）でスケールした。

### 検証結果

- コース全長: 4455 → 6300ワールド単位（カーブが大幅に伸びたため）
- 最高速度のみでの理論最速タイム: 70秒（60〜90秒の目標レンジのより中央寄りに）
- 実際に加速を含めてシミュレーション: 73.87秒
- 無操舵・アクセル全開でのカーブ通過時の最大ズレ: 0.299（変更前0.277とほぼ同水準、安全マージン維持を確認）
- HUDのSPEED表示: 最高速度到達時に正しく320と表示されることを確認

`tests/test_road.py`の所要時間チェックとworld_xの範囲チェックが、今回の意図的な変更（カーブを大きく緩く・長くしたことでコースの横幅も理論所要時間も増える）によって従来の閾値を超えて一度失敗した。これは実装のバグではなく、意図した設計変更の結果だったため、チェックの閾値・考え方を「フラットアウト時のタイムが60〜90秒の目標に収まっているか」を主眼にする形に見直した。

`pytest`40件全て成功。ヘッドレスレンダリングで、道路が画面奥まで大きくゆったりと弧を描く「ハイウェイの緩やかなカーブ」らしい見た目になっていることを確認した。

## 2026-09-03: 道路の起伏（坂・丘・谷）を追加

ユーザーより、既存の左右カーブに加えて「上り坂→頂上→下り坂」「緩やかな谷」といった縦方向のコース変化を、OutRun系疑似3D表現で実装してほしいという依頼。要件は以下の通り：

- 道路・敵車・路肩オブジェクトが画面上で上下することで起伏を表現する（単純な画面全体の平行移動ではなく、セグメント単位の高さ情報を投影処理へ反映）
- 上り→頂上→下りは複数セグメントにわたって滑らかに変化（急な折れ曲がり禁止）
- 頂上の向こう側にある道路・敵車は隠れ、頂上を越えると現れる（遮蔽）
- プレイヤー車は画面下部基準のまま、坂に応じて控えめに上下（ジャンプ・サスペンション物理は不要）
- 左右眼で垂直位置は完全に一致させる（垂直視差を作らない）
- 中盤の一区間に追加し、既存コースは作り直さない。カーブと坂の組み合わせも一部入れるが、急カーブ×急勾配の組み合わせは避ける

### データ構造：`Segment.elevation` + チェックポイント方式

`road.py`の`Segment`に`elevation`（world_y、路面の高さ）を追加。カーブ（`curve`→`world_x`）が1セグメントずつ積分していく方式なのに対し、標高は意図的に別方式にした：`(segment_index, elevation)`のチェックポイント列を`apply_elevation()`でsmoothstep（`3t²-2t³`、ease-in/ease-out）補間する。これにより

- 各丘・谷の開始/終了インデックスを明示的に指定できる（「このカーブの途中から下りが始まる」といった配置がしやすい）
- 補間そのものが必ずease-in/ease-outになり、折れ曲がりが原理的に発生しない
- カーブの自己積分のように誤差が長距離にわたって蓄積する心配がない

チェックポイントは`_hill_checkpoints()`で、丘（上り90segs→頂上30segs→下り150segs、高さ+14.0）と谷（下り50segs→底15segs→上り50segs、深さ-8.0）から生成。丘の下りは、次のカーブ区間（medium right sweep, peak 0.14）の前半に意図的に重なるよう配置し、「カーブと坂の共存」を確認できるようにした。一方、谷はシケイン（peak 0.32、最も急なカーブ）の手前5segs以上の余白を空けて完全に独立させ、急カーブ×急勾配の組み合わせは避けた。

安全上限として`MAX_GRADE = 0.09`（世界座標1単位あたりの標高変化の上限）を定義し、`tests/test_road.py`で全区間がこれを超えないことを検証。実際の丘の勾配は上り0.052・下り0.031、谷は0.053で、いずれも上限の半分〜6割程度に抑えてあり、将来さらに勾配をきつくする余地を残した（`MAX_SPEED`をこれまで段階的に上げてきたのと同じ考え方）。

`elevation_at()`を`world_x_at`/`curve_at`と同じ補間方式（セグメント境界間の線形補間）で追加し、任意のworld_z（敵車のz、プレイヤーのzなど、セグメント境界に一致しない値）でも滑らかに標高を取得できるようにした。

### 投影処理：`project()`にworld_y/cam_yを追加

`game.py`の`project(world_x, world_z, cam_x, cam_z, width, height)`を`project(world_x, world_y, world_z, cam_x, cam_y, cam_z, width, height)`に拡張。画面Y座標の計算式を

```
sy = height/2 + scale * (CAMERA_HEIGHT + (cam_y - world_y) * ELEVATION_Y_SCALE) * height/2
```

に一般化した。標高が全区間0（従来通り平坦）の場合、`cam_y - world_y = 0`となり式は元の`sy = height/2 + scale*CAMERA_HEIGHT*height/2`と完全に一致する——つまり平坦区間・既存の全テスト・既存の見た目は数学的に無変更のまま拡張できている（`ELEVATION_Y_SCALE`は「起伏による画面Y方向の倍率」の調整用定数）。

道路・敵車・路肩デコレーションのいずれの描画箇所も、この同じ`project()`を経由するよう統一した。敵車・デコレーションは`elevation_at()`で自分のいるworld_zの標高を取得して`project()`に渡すため、「路面から浮く/沈む」ことなく、所属セグメントの高さに正確に追従する。

左右眼の`sy`は常に同一の値を共有し（既存のステレオレンダラーの設計どおり、垂直方向の視差は一切作らない）、水平方向のみ`StereoRenderer.project_x`で depth ごとに別々に計算する既存の仕組みをそのまま維持した。

### 頂上での遮蔽：描画順の反転 + crest watermarkクリップ

これが今回でもっとも技術的に難しかった部分。従来の`_draw_road()`は「奥→手前」の順でセグメントのポリゴンを塗り重ねるペインターズアルゴリズムだった（平坦・カーブのみなら、これで十分正しく見える）。しかし丘がある場合、この順序のままだと「頂上の向こう側の道路」が本来隠れているべきなのに、後から重ね塗りされて突き抜けて見えてしまう。

対応として、有名なOutRun系疑似3Dレーサーの手法（Jake Gordon氏のチュートリアル実装で知られる）を採用し、描画順を「手前→奥」に反転した上で、以下のクリップ処理を追加した：

- `crest_y`という「ここまで描画したセグメントの中で最も奥側（画面上方）に到達したY座標」のウォーターマークを、手前から奥へ進みながら更新
- あるセグメントの奥端のY座標が、(a)そのセグメント自身の手前端のY座標以上、または (b) 現在のウォーターマーク以上——のどちらかであれば、そのセグメントは「丘に隠れている」と判定して描画をスキップ
- スキップしなかった場合のみ、`crest_y`をそのセグメントの手前端Yで更新

平坦・カーブのみの区間では、Yが単調減少するため上記のスキップ条件が数学的に一度も成立せず、従来の見た目と完全に同じ結果になることを確認済み（このため描画順の反転は既存のテスト・見た目に影響しない）。

敵車・路肩デコレーションについても同じウォーターマーク列（`clip_before_n`、セグメントごとに「描画前の時点でのウォーターマーク値」を保存）を`_sprite_visible()`で参照し、丘の向こうにいる敵車・木も同じ基準で隠す/表示するようにした。手動検証で、プレイヤーが丘の中腹にいる間は頂上の向こうの敵車が非表示、頂上を越えると表示に切り替わることを確認（`tests/test_game.py`の`test_object_behind_hill_crest_is_hidden_until_crested`として固定化）。

### プレイヤー車とカメラ：2つの独立した緩やかな追従

- `Game.cam_elevation`：カメラの路面高基準。プレイヤーの現在地の標高へ`CAMERA_ELEVATION_SMOOTHING`（1秒あたりの追従率）で指数的に緩和しながら追従。瞬時に切り替わらないため、丘の頂上・谷底で画面全体が唐突にジャンプすることがない。
- `Game.player_bob`：プレイヤー車スプライトのみの、控えめな上下オフセット（px）。`PLAYER_BOB_LOOKAHEAD`先までの局所勾配から目標値を計算し、`PLAYER_BOB_STRENGTH`で強さを調整、`PLAYER_BOB_MAX_PX`で振れ幅を強制的に制限、`PLAYER_BOB_SMOOTHING`で滑らかに追従させる。車の基準位置（画面下部、`h - 34`）自体は変えず、その上にこの小さなオフセットを乗せるだけなので、「画面下部で安定しつつ、坂に応じて少し動く」という要件どおりになっている。

いずれもジャンプ・サスペンションの物理演算は行わず、単純な指数的スムージング（`x += (target - x) * min(1, rate * dt)`）のみ。

### 調整可能な値（`config.json`とは明確に分離）

すべて`game.py`のモジュール定数として追加。**`config.json`（viewport/eye_gap/swap/flip等の実機キャリブレーション値）には一切触れていない**——ゲームプレイ/見た目のパラメータのみを分離して追加した。

| 定数 | 意味 |
|---|---|
| `ELEVATION_Y_SCALE` | 起伏による画面Y方向の倍率 |
| `CAMERA_ELEVATION_SMOOTHING` | カメラの路面高追従速度 |
| `PLAYER_BOB_LOOKAHEAD` / `PLAYER_BOB_STRENGTH` / `PLAYER_BOB_MAX_PX` / `PLAYER_BOB_SMOOTHING` | プレイヤー車の上下動の強さ・追従速度・上限 |
| `road.HILL_HEIGHT` / `VALLEY_DEPTH` | 上り坂・下り坂（丘・谷）の強度 |
| `road.HILL_RISE_SEGMENTS` / `HILL_CREST_SEGMENTS` / `HILL_FALL_SEGMENTS` / `VALLEY_DOWN_SEGMENTS` / `VALLEY_HOLD_SEGMENTS` / `VALLEY_UP_SEGMENTS` | 高さ変化に使用するセグメント数 |
| `road.MAX_GRADE` | 最大勾配（安全上限、テストで検証） |

### 検証結果

- `pytest`: 既存40件 + 新規11件（`test_road.py`に6件、`test_game.py`に5件）= 51件全て成功
- ヘッドレスでコースを最高速度でフラットアウト走行し、スタートからゴールまでクラッシュなく完走することを確認（`player.z`がtrack_length に到達、`finished=True`）。所要時間は従来と変わらず約74秒（坂の追加はコース全長・速度チューニングには影響しない設計のため）
- 丘の各区間（平坦→上り→頂上手前→頂上越え→下り中腹→谷底）でフレームを1枚ずつ保存し目視確認：上り坂で道路が画面上方へせり上がり、頂上付近では奥の道路がほぼ見えなくなり、下り坂では道路が低い位置に伸びる見た目になっていることを確認。左右画像は上下方向で完全に一致（垂直視差なし）
- カーブと下り坂が重なる区間（丘の下り後半×medium right sweepカーブの前半）でも、道路の幅や輪郭が破綻せずに描画されることを確認
- 頂上を挟んだ遮蔽テスト：プレイヤーが丘の中腹にいる時、頂上の向こう側（下り区間内）にある地点は非表示、頂上を十分越えた後は表示に切り替わることを確認（`_sprite_visible`を直接呼び出すユニットテストとして固定化）
- ヘッドレスでのフレーム処理時間は平均1.4ms・p95 1.6ms・最悪3.6ms（WSLのダミードライバ上の数値であり、実機60fps確認の代わりにはならない——**実機での60fps確認はユーザーにお願いする必要がある**）
- `config.json`は本作業を通じて一切変更していないことを`git diff`で確認済み

### 実機で確認してほしいこと

このセッションには実機がないため、以下は必ずユーザー自身の目で確認をお願いしたい：

1. 中盤（コース全長6300のうち、およそ3285〜4095あたり）で実際に丘・谷を体感し、「上り・頂上・下り」「谷」が視覚的に判別できるか
2. 頂上の向こうの敵車が本当に見えず、頂上を越えると現れるか（実機の視野・レンズ越しでは印象が変わる可能性がある）
3. プレイヤー車の上下動が「控えめ」に感じられるか、強すぎる/弱すぎる場合は`PLAYER_BOB_STRENGTH`等の調整が必要か
4. 実機で60fpsを維持できているか（`D`キーのデバッグ表示で確認可能）
5. 左右眼で坂の見え方に上下のズレを感じないか

## 2026-09-03: 坂の強調・早期化、カーブの「うねうねS字」化（ユーザーフィードバック対応）

ユーザーより2点のフィードバック：

1. 「坂のアップダウンはもっと強調して、さらに早めに出てくるといいですね」
2. 「斜め方向の直線の長いカーブは走っていてあまり気持ちがよくないので、うねうねのS字カーブが多いほうが気持ちが良さそう」

### 1. 坂をより強調・より早く

- `road.py`の`HILL_START`を`1095`（コース全長6300のうち約52%地点）から`350`（約17%地点、最初のカーブ区間の直後）へ変更。以降のプレイでかなり早い段階で丘に到達するようになった。
- `HILL_HEIGHT`を`14.0→20.0`、`VALLEY_DEPTH`を`8.0→11.0`に引き上げ、標高変化そのものを強調。
- `game.py`の`ELEVATION_Y_SCALE`（起伏による画面Y方向の倍率、純粋な描画スケール定数で世界座標のジオメトリやMAX_GRADEの安全マージンには一切影響しない）を`2.4→3.6`に引き上げ、画面上での見た目のインパクトをさらに強調。
- `HILL_HEIGHT`引き上げに伴い、`apply_elevation`のsmoothstep補間の**瞬間最大勾配**（区間平均勾配の1.5倍になる — 3t²-2t³の導関数6t(1-t)の最大値が1.5であるため）が旧`MAX_GRADE=0.09`をわずかに超過（実測ピーク約0.111）したため、`MAX_GRADE`を`0.09→0.12`へ引き上げ。安全上限を単純に緩めたのではなく、**平均勾配ではなくピーク勾配を基準にした値へ計算方法自体を明確化**した上での調整（コメントに明記）。

VALLEY_STARTは変更していない（下記カーブ変更で各区間のセグメント境界インデックスをすべて元と同一に保ったため、再配置の必要がなかった）。

### 2. 長い緩やかなカーブを「うねうねS字」に分割

`road.py`の`_add_bend()`は元々「0→+peak→0→-peak→0」という1回の完全なサイン波（それ自体が数学的には1つの右左Sカーブ）を生成する仕組みだったが、既存の主要カーブ（`0.09/210`、`-0.11/240`、`0.10/290`、`0.14/190`、`-0.10/190`）はいずれも**低いピーク×長い距離**で意図的にチューニングされていたため、方向転換が非常に緩やかで「長い斜めの直線」のように感じられていた。

対応として、シケイン（`0.32/110`、ユーザーからの指摘対象外）を除く上記5つのカーブイベントを、**同じ合計セグメント数を保ったまま**、3〜4個の短い交互符号の`bend`イベントの連続に分割した（例：`0.09/210`一発 → `[0.09/70, -0.09/70, 0.09/70]`）。合計長を完全に一致させたことで、以降のTRACK_EVENTSのセグメントインデックス境界（＝丘・谷の配置計算の前提）は一切変わっていない。

各ロブは元のピーク値をそのまま使うため、1ロブあたりの「長さ×ピーク」の積（安全マージンの目安）はロブ数で分割された分だけ小さくなる——つまり理屈の上ではむしろ従来より安全マージンに余裕が生まれる方向の変更である。

### 検証結果

- `pytest`: 既存51件（前回の起伏実装で追加した11件含む）全て成功。カーブ変更後に`test_elevation_never_exceeds_max_grade`が一度失敗し（ピーク勾配が0.09をわずかに超過）、`MAX_GRADE`を0.12へ調整することで解消——バグではなく、意図した強度アップに伴うテスト閾値の見直しとして対応（過去のカーブ調整時と同じパターン）。
- 無操舵・アクセル全開でのフラットアウト走行シミュレーション：新しいカーブ構成でもコース全長・完走時間は変化なし（6300ワールド単位、約74.10秒）。最大横方向ズレは**0.2987**——変更前の0.28〜0.30という記録値とほぼ同一で、安全マージンが劣化していないことを確認。
- ヘッドレスレンダリングで新しいカーブ区間・早期化した丘の各段階（丘の直前・上り・頂上手前・頂上越え・下り×新カーブ重複区間）をPNG保存して目視確認：明確に蛇行するS字の道路形状になっていること、丘がこれまでよりずっと早い段階で、かつ画面上でより大きく起伏して見えることを確認。
- `python main.py --race --test-frames 60`（ヘッドレス60フレーム実行）：クラッシュなく正常終了。
- 頂上遮蔽のユニットテスト（`test_object_behind_hill_crest_is_hidden_until_crested`）はコード内で`road.HILL_START`を動的に参照しているため、閾値の手動修正なしに新しい配置（index 350）へ自動的に追従して成功。
- HUD・衝突・タイムアップ・リスタート関連の既存テスト（`test_collision_with_traffic_car_reduces_speed`、`test_time_running_out_sets_time_up`、`test_restart_resets_state`、`test_every_key_binding_executes_without_crashing`等）は全て変更なしで成功——今回の変更が既存機能に影響していないことを確認。
- `config.json`は本作業でも一切変更していないことを`git diff`で確認済み。

### 実機で確認してほしいこと（追加分）

1. 坂の起伏が「もっと強調された」と感じられるか、まだ物足りなければ`HILL_HEIGHT`/`ELEVATION_Y_SCALE`のさらなる調整が可能
2. 丘がコース序盤（約17%地点）で早すぎる/ちょうど良いか
3. 新しいカーブが「うねうねのS字」として気持ちよく感じられるか、まだ緩すぎる/長すぎる場合はロブ数を増やす・ピークを上げるなどのさらなる調整が可能

## 2026-09-03: BGM選択画面（SELECT MUSIC）を追加

ユーザーが`bgm/`フォルダに3曲のmp3（`Pixel_Breeze.mp3` / `Crimson_Highway.mp3` / `Beyond_the_red_horizon.mp3`）を配置し、レース開始前にBGMを選ぶ画面の追加を依頼。要件：

- 画面中央に「SELECT MUSIC」、その下に「◀ 曲名 ▶」を横一列表示
- 最初は「PIXEL BREEZE」選択状態で試聴再生
- 左右キーで切り替え（両端でラップアラウンド）、切り替え時は現在の曲を止めて新しい曲を先頭から再生、可能なら短いフェード
- スタートキーで確定してゲーム開始、レース中も選択曲を先頭からループ再生
- 既存のStereoRenderer経由で左右画面に表示（視差ゼロ）、実機キャリブレーション値（viewport等）は変更しない
- 音源が見つからない/読み込めない場合もクラッシュせず、エラーを表示してBGMなしで続行

### 実装

新規モジュール`music.py`を追加（`game.py`から利用、循環importなし）：

- **`MusicPlayer`**：`pygame.mixer.music`（SDL_mixerの単一グローバル再生チャンネル）のラッパー。`TRACKS`定数に`[("PIXEL BREEZE", "Pixel_Breeze.mp3"), ("CRIMSON HIGHWAY", "Crimson_Highway.mp3"), ("BEYOND THE RED HORIZON", "Beyond_the_red_horizon.mp3")]`の順で保持。`select`/`next`/`prev`（`index % len(tracks)`によるラップアラウンド）、`play_selected(loop, fade_ms)`（読み込み→再生。`pygame.error`/`OSError`を捕捉し、`last_error`に短いメッセージ（ファイル名＋簡潔な理由。生の例外文字列や長いパスは表示に使わない——後述の理由）を記録して`False`を返すのみで、例外を再送出しない）、`switch_to_selected_preview`（選択曲をループでプレビュー再生、フェードイン250ms）、`start_looping`（レース開始時、選択曲を先頭からループ再生、フェードイン400ms）を提供。
- **`MusicSelectScreen`**：「SELECT MUSIC」タイトル・左三角形（`pygame.draw.polygon`によるベクター描画、Unicode三角形記号のフォント依存を避けた——プロジェクトの「全て図形描画、外部素材に頼らない」方針に合わせた）・曲名・右三角形・下部の操作ヒント・（エラー時のみ）エラー行を`renderer.draw_flat`経由で描画。左右キー（`←`/`A`、`→`/`D`）で`prev`/`next`＋プレビュー再開、`Enter`/`Space`で確定して選択インデックスを返す、`Esc`で`None`を返す（ゲーム全体を終了）。ヘッドレス`--test-frames`モードでは実入力を待たずに数フレーム描画してから自動的に最初の曲（インデックス0）で確定する（`main.py --race --test-frames N`のNは従来通りレース本体のフレーム数として使われる設計を維持するため）。
- **`game.py`統合**：`Game.__init__`が`screen`/`renderer`/`music`を任意引数として受け取れるよう変更（未指定なら従来通り自前で生成——既存のテスト・呼び出しは無変更で動作）。`music`が渡された場合、コンストラクタ内で`music.start_looping()`を呼び、レース開始と同時に選択曲のループ再生に切り替える。モジュールレベルの`run()`が「画面作成→`MusicSelectScreen`実行→確定したインデックスを`music.select()`で反映→`Game`起動」という流れに変更。

### 見つけて直したバグ

1. **`game.run()`が選択結果を反映していなかった。** `MusicSelectScreen.run()`の戻り値（確定インデックス）を受け取っていたが、それを`music.select()`で明示的に反映せず、`music`オブジェクト内部の状態（実装上は選択操作のたびに更新されているため、実際には常に一致する）に暗黙的に依存していた。モック化したテスト（`MusicSelectScreen.run`を差し替えて戻り値だけ変える）でこの脆さが発覚——`music.select(selected)`を明示的に呼ぶよう修正し、戻り値と内部状態が食い違っても正しく動作するようにした。
2. **長いファイル名/パスでのエラー表示が読めなくなる。** 当初`last_error`に生の例外メッセージ（フルパス込み）をそのまま入れていたところ、実機viewport幅（約280px）を大きく超え、中央揃えのため両端が切れて文字化けのように読めなくなることをPNG目視確認で発見。(a) `last_error`自体をファイル名＋簡潔な理由（例："not found"）に短縮、(b) 描画側にも`_fit_text()`という省略記号（…）付きの防御的トリミングを追加——万一極端に長いファイル名が使われても表示が破綻しないようにした。

### 検証結果

- 3曲すべての読み込み・再生をヘッドレス（`SDL_AUDIODRIVER=dummy`）で確認（`pygame.mixer.music.load`/`play`とも成功）
- 左右キーでの切り替え・両端でのラップアラウンド（右端→右で先頭へ、左端→左で末尾へ）を、`MusicPlayer`の直接呼び出しと、実際のキーイベントループ（`pygame.event.get`をスクリプト化したキー入力で駆動）の両方で確認
- 存在しないファイルを指定した場合、クラッシュせず`last_error`が設定され、BGMなしでプレビュー・レース続行できることを確認（`MusicPlayer`単体・`Game`統合の両方）
- `Enter`/`Space`で確定してゲーム開始、`Esc`で終了（`None`が返り、`game.run()`がレースを開始せず正常終了）を確認
- レース開始後、選択曲が`loops=-1`でループ再生に切り替わることを確認（`pygame.mixer.music.get_busy()`で再生中を確認）
- `python main.py --race --test-frames 60`：クラッシュなく正常終了（SELECT MUSIC画面→自動確定→レース60フレームの流れを通過）
- `pytest`：既存70件（本機能のテスト19件含む——`tests/test_music.py`15件、`tests/test_game.py`4件）全て成功
- `config.json`は本作業でも一切変更していないことを`git diff`で確認済み

### 実機で確認してほしいこと

1. 「SELECT MUSIC」画面のレイアウト（タイトル・三角形・曲名）が左右で綺麗に一つに見えるか（垂直・水平ともズレがないか）
2. 左右キーでの曲切り替え・試聴再生が実機のスピーカー/ヘッドホンで自然に聞こえるか、フェードが機能しているか
3. スタートキー（`Enter`または`Space`）でのゲーム開始、レース中のループ再生に違和感がないか
4. （もし発生すれば）BGM読み込みエラー時の表示が実機視野で読めるか

## 2026-09-03: エンジン音・タイヤ軋み音のSEを追加

ユーザーから「走行中はスピードにあわせてエンジン音、カーブではタイヤがキキーと軋む音をSEとして入れたい」との依頼。BGMと違い音源ファイルは用意されていなかったため、実装方式についてユーザーに確認：(a) コードで波形合成（推奨案として提示）、(b) SE用の音声ファイルを別途用意してもらう、の2択を提示し、**(a) コードで合成**を選択いただいた。理由として、速度に応じた連続的なピッチ変化はプロシージャル合成の方が素直に実現できること、既存の「全て図形描画、外部素材に頼らない」というプロジェクト方針とも一貫することを確認した。

### 実装

新規モジュール`sfx.py`を追加（`numpy` + `pygame.sndarray`で波形をゲーム開始時に一度だけ合成し、`pygame.mixer.Channel`で再生・クロスフェード・フェードアウトを制御。ファイルI/Oは一切なし）：

- **`EngineSound`**：加算合成（基音+倍音5本、`ENGINE_HARMONICS`）によるノコギリ波的なエンジンドローン音を、`ENGINE_BUCKET_COUNT=16`段階のピッチ（`ENGINE_MIN_FREQ=70Hz`アイドル〜`ENGINE_MAX_FREQ=240Hz`レッドライン）で事前合成。`pygame.mixer.music.play()`のようにピッチをリアルタイムで連続変化させる機能がpygameには無いため、離散ピッチバケットを`speed_frac`（`player.speed / MAX_SPEED`）に応じて選び、バケットが変わるたびに2つの交互チャンネル（`Channel(0)`/`Channel(1)`）間で90msクロスフェードして切り替える方式（16段階あれば体感上は連続的なピッチ変化に感じられる）。各バケットのループ波形は、ループ境界でクリック音が出ないよう「バッファ内にちょうど整数周期分の波形が収まる」ようピッチをスナップして生成（`_engine_wave`）。
- **`TireScreech`**：ノイズを一次差分でハイパス的に加工したもの＋ビブラート付きの共鳴音2本を混ぜた「キーキー」音を一度だけ合成（`_tire_screech_wave`）。既存の`update()`内で毎フレーム計算済みの遠心力ドリフト補正の元になっている値`abs(current_curve) * speed_frac`を「横方向の力」の代用指標として再利用し、`TIRE_SCREECH_THRESHOLD=0.15`を超えたら専用チャンネル（`Channel(2)`）で再生開始、既に再生中なら再トリガーしない（連続したカーブ通過中は途切れず鳴り続ける）、閾値を下回ったら120msでフェードアウトする方式。
- **`game.py`統合**：`Game.__init__`で`self.engine_sound = EngineSound()` / `self.tire_screech = TireScreech()`を常時生成（BGMのような選択画面はなく、走行中は常時鳴る設計）。`Game.update()`内、衝突判定の直後（この時点で衝突による速度低下も反映済みの`player.speed`が確定しているため、ヒットした瞬間のピッチ変化も1フレーム遅れず聞こえる）に`engine_sound.update(...)`/`tire_screech.update(...)`を呼び出す。レースが終了（フィニッシュ/タイムアップ）した次のフレームで`racing=False`側の分岐に入り、両SEを自動フェードアウトさせる。

### 安全性（クラッシュしないことの担保）

`music.py`と同じ設計哲学を踏襲：`EngineSound`/`TireScreech`のコンストラクタで`pygame.mixer.get_init()`が`None`（ミキサー未初期化）の場合、または`numpy`のimportに失敗した場合、`available=False`となり以降の`update()`/`stop()`呼び出しはすべて無音の no-op になる（例外は投げない）。合成・再生処理自体も`pygame.error`/`ValueError`を捕捉するようにしており、SEはあくまで演出の追加要素で、これが原因でレース本体がクラッシュすることは無い。

### 検証結果

- `pytest`：既存91件のうち新規22件（`tests/test_sfx.py`18件、`tests/test_game.py`統合4件）を追加、全て成功
- エンジン音のピッチバケットが速度0→最高速で単調増加すること、バケット変更時に交互チャンネルでクロスフェードされることを確認
- タイヤ軋み音が閾値超過で再生開始、再生中は再トリガーしない、閾値を下回ると（実時間待機で確認）フェードアウトして無音になることを確認
- ミキサー未初期化・numpy未使用（`monkeypatch`でシミュレート）のいずれでもクラッシュせず`available=False`になることを確認
- 無操舵・アクセル全開でコース全体（約74秒、4446フレーム）をヘッドレスでシミュレーションし、クラッシュなく完走。エンジンのピッチバケット変更が130回、タイヤ軋み音が鳴っているフレームが全体の12.5%（うねうねS字化後も0.15閾値を超えるのは主に終盤のシケイン付近であることを確認——想定通り、緩いカーブでは鳴らず、鋭いカーブでのみ鳴る設計）
- 衝突・タイムアップ・フィニッシュ・リスタート時の挙動（SEの自動停止・リスタート後の再開）を確認。既存のHUD・衝突・タイムアップ・リスタートのテストは無変更のまま全て成功
- `python main.py --race --test-frames 60`：クラッシュなく正常終了
- `requirements.txt`に`numpy>=1.24`を追加（波形合成に必須の新規依存）
- `config.json`は本作業でも一切変更していないことを`git diff`で確認済み

### 実機で確認してほしいこと

1. エンジン音のピッチが速度に応じて自然に変化して聞こえるか（16段階の切り替えが不自然に感じられないか）
2. タイヤの軋み音が「カーブでキキーと鳴る」感覚に合っているか、音量・音色（合成音のため、実機で聞くと想定と違う印象になる可能性が高い）
3. BGMと同時に鳴らした際の音量バランス（`ENGINE_VOLUME=0.32`、`TIRE_SCREECH_VOLUME=0.45`は暫定値、実機で聞いた上で調整が必要な可能性が高い）
4. 閾値`TIRE_SCREECH_THRESHOLD=0.15`が「カーブでの軋み」として適切なタイミングで鳴っているか（緩すぎる/鳴らなすぎる場合は調整）

## 2026-09-04: エンジン音・タイヤ軋み音が小さすぎる問題を修正

実機で試したユーザーから「エンジンやタイヤ音はほとんど聞こえないですね」とのフィードバック。

### 原因調査

数値で確認したところ、問題は「音量設定が小さい」というだけでなく、**合成波形そのもののRMS（実効的な音の大きさ）がそもそも低かった**ことが判明した：

- エンジン音：倍音を5本足し合わせて正規化した波形は、ピーク振幅こそ0.67程度あるが、RMSは0.37（デジタルフルスケール比）程度しかない——単一のサイン波（RMS≈0.71×振幅）よりもさらに低い。ここに`ENGINE_VOLUME=0.32`を掛けると、実効RMSはわずか**0.119**（フルスケール比、約-18.5dBFS相当）にしかならない。BGMのようにマスタリングされた音楽トラックと比べて、この値は明らかに小さすぎる。
- タイヤ軋み音も同様の計算で実効RMS**0.118**と、ほぼ同じレベルで小さかった。

音量パラメータ自体（`sound.set_volume()`）が正しく機能しているかも`pygame.mixer.Sound.get_volume()`で確認したが、設定通りに反映されており、ミキサー側のバグではなく「波形設計自体が地味すぎた」ことが根本原因と判断した。

### 対応

1. **音量を大幅に引き上げ**：`ENGINE_VOLUME`を`0.32→0.8`、`TIRE_SCREECH_VOLUME`を`0.45→0.8`に。
2. **ソフトクリップ（tanh飽和）による「音圧」の底上げ**：`_soft_clip(x, drive) = tanh(x*drive)/tanh(drive)`という、ピークを1.0に保ったまま中間振幅のサンプルを天井付近まで持ち上げる古典的な手法を追加。エンジン音には`ENGINE_SATURATION_DRIVE=2.5`（やや強め、迫力を出す）、タイヤ軋み音には`TIRE_SCREECH_SATURATION_DRIVE=1.6`（弱め——ノイズ系の音を強くかけすぎると「キーキー」感が失われ、ただの潰れたノイズになるため）を適用。
3. デバッグオーバーレイ（`D`キー）に、エンジンの現在のピッチバケットとタイヤ軋み音の再生中フラグを表示する行を追加。今後実機で確認する際、「音量が小さいだけなのか」「そもそも鳴っていない（トリガー自体の問題）」かを目視で切り分けられるようにした。

### 検証結果（数値シミュレーション）

- エンジン音の実効RMS：0.119 → **0.525**（約4.4倍）
- タイヤ軋み音の実効RMS：0.118 → **0.319**（約2.7倍）
- いずれもピーク値は1.0未満（エンジン0.954、タイヤ0.900）に収まっており、ソフトクリップによるハードクリップ/割れは発生しない設計
- `pytest`：既存91件全て成功（波形の値レンジ・ループの継ぎ目なし等を検証する既存テストは、ソフトクリップ適用後も条件を満たすことを確認）
- `python main.py --race --test-frames 60`：クラッシュなく正常終了
- `config.json`は本作業でも一切変更していないことを`git diff`で確認済み

### 実機で確認してほしいこと

1. エンジン音・タイヤ軋み音が今度こそはっきり聞こえるか
2. 音量バランスがBGMに対して大きすぎ/ちょうど良いか（`ENGINE_VOLUME`/`TIRE_SCREECH_VOLUME`はさらに調整可能）
3. ソフトクリップによる音の歪み・不快感がないか（`ENGINE_SATURATION_DRIVE`/`TIRE_SCREECH_SATURATION_DRIVE`を下げれば歪みは弱まる）
4. まだ聞こえない場合は、`D`キーのデバッグオーバーレイで`engine: on bucket=X/15`・`tire_screech: on playing=True/False`が表示されるか確認してほしい——ここが正しく変化していれば「トリガーは動いているが音量/出力側の問題」、変化していなければ別の原因（ミキサー初期化失敗など）を疑う必要がある

## 2026-09-04: BGMの初期音量が未設定だった問題を修正

作業再開時、`docs/NEXT_STEPS.md`と現在のコードを確認したところ、BGM選択・再生機能（`music.py`）自体は前回で完成していたが、**BGMの再生音量を明示的に設定する処理が一つも無い**ことに気づいた（SE側の`sfx.py`は`ENGINE_VOLUME`/`TIRE_SCREECH_VOLUME`を持つのに対し、BGM側には相当するものが無かった）。

### 原因

`MusicPlayer.play_selected()`は`pygame.mixer.music.load()` → `pygame.mixer.music.play()`のみで、`pygame.mixer.music.set_volume()`を一度も呼んでいなかった。`pygame.mixer.music`はデフォルトでフルスケール（1.0＝100%）で再生されるため、試聴・レース中とも常に100%音量になっていた。

### 対応

- `music.py`に`BGM_VOLUME = 0.65`（60〜70%指定の中央値）を追加。
- `play_selected()`内、`pygame.mixer.music.play()`を呼ぶ**前**に`pygame.mixer.music.set_volume(BGM_VOLUME)`を呼ぶよう変更。`play()`の後ではなく前に設定しているのは、フェードイン（`fade_ms`）は「無音から`set_volume`で設定した値まで」ランプする仕様のため、後で呼ぶとフェード中に音量が瞬時に飛んでフェード効果が失われてしまうことを避けるため。
- 回帰テストを`tests/test_music.py`に2件追加：`BGM_VOLUME`が60〜70%の範囲内であることの定数チェックと、実際に`play_selected`した後`pygame.mixer.music.get_volume()`が`BGM_VOLUME`になっていることの確認（SDL_mixerの内部量子化があるため許容誤差0.01で比較）。

### 検証結果

- `pytest`：既存91件＋新規2件、計93件すべて成功
- `config.json`は本作業でも一切変更していないことを`git diff`で確認済み

### 実機で確認してほしいこと

1. SELECT MUSIC画面での試聴、レース中のBGMいずれも、体感で「大きすぎず小さすぎない」65%程度に聞こえるか（エンジン音・タイヤ軋み音とのバランスも含む）
2. 感覚と違えば`BGM_VOLUME`（`music.py`）を調整可能

## 2026-09-04: Maker Faire展示用「RESET」機能を追加

来場者が交代するたびにアプリを終了・再起動しなくても済むよう、「アプリを終了せず起動直後の状態へ戻せる」Reset機能を追加した（既存のRestart＝同じBGM/設定のまま現在のレースをやり直す、とは明確に別の操作）。

### 現状確認（実装前）

- 画面状態管理: `game.run()`（モジュールレベル）は「SELECT MUSIC → `Game.run()`」を一直線に呼ぶだけで、SELECT MUSICへ「戻る」経路自体が存在しなかった。`Game.run()`はメインループの最後に無条件で`pygame.quit()`を呼んでいた。
- Restart（`R`キー）: `Game._restart()`はplayer/タイマー/スコア/衝突/`traffic`/`cam_elevation`/`player_bob`をクリアするが、BGM・SEには触れない。
- 入力割り当て: キーボードは矢印/WASD・`[` `]`・`Z`・`I`・`D`・`F`・`S`・`R`・`Esc`が使用済みで、**`Backspace`は未使用**。ゲームパッド対応は`pygame.joystick`/`pygame.controller`関連コードが一切存在せず、**新規実装**であることを確認した。

### ゲームパッドのボタンマッピングについてユーザーに確認

「実機で使うゲームパッドの機種が不明なままボタン番号を決め打ちできない」旨を報告し、ユーザーに選択してもらった結果、「標準的なXbox/PS系コントローラーを想定し、SDL_GameController API（`pygame`の`CONTROLLER_BUTTON_BACK`等、機種非依存の名前空間）で実装する」方針で合意。実機がまだ無くても先に実装を進め、実機到着後にボタン割り当てだけ確認すればよい設計とした。

### 対応

- 新規`input_reset.py`：`music.py`と`game.py`の両方から使う、Select/Backボタンの「約1秒長押しでReset」判定を共通化した`GamepadResetHold`クラスと、接続済み/後から接続されたゲームパッドを安全に開く`open_connected_controllers()`/`open_controller_from_event()`を実装。ゲームパッドが存在しない・認識されない環境でも例外を投げない設計（`pygame.error`を握りつぶす）。
- `Game._trigger_reset()`（新規）：既存`_restart()`を呼んでレース状態（速度・位置・スコア・タイマー・衝突状態・敵車配置・カメラ関連の補間値）をクリアしつつ、追加でエンジン音/タイヤ軋み音のSEを停止（`engine_sound.stop()`/`tire_screech.stop()`）、BGMを停止（`MusicPlayer`に新規追加した`stop()`）し、`self._outcome = "reset"`をセットする。
- `Game._handle_keydown`に`Backspace`を追加（即時Reset、`_trigger_reset()`を呼んで`False`を返しループを終了）。
- `Game.run()`のメインループへゲームパッドイベント（`CONTROLLERBUTTONDOWN`/`UP`/`CONTROLLERDEVICEADDED`）の処理を追加し、毎フレーム`GamepadResetHold.triggered()`をチェック（約1秒保持で発火、離せばキャンセル）。
- `Game.run()`は末尾の`pygame.quit()`を廃止し、代わりに`"quit"`／`"reset"`のoutcome文字列を返すよう変更。`"reset"`の場合のみ、`renderer.draw_flat`経由（視差ゼロ、SELECT MUSIC画面と同じ方式で左右に描画）で「RESET」を一瞬（150ms）表示してから返す。
- モジュールレベルの`game.run()`を`while True:`ループに変更：SELECT MUSIC → `Game.run()`を実行し、outcomeが`"reset"`ならウィンドウ・`renderer`・`MusicPlayer`インスタンスを使い回したままループを継続、`"reset"`以外（`"quit"`）でループを抜けて`pygame.quit()`を一度だけ呼ぶ。`cfg`（config.json由来のオブジェクト）は一度読み込んだものをそのまま使い回し、再読み込み・再保存は一切行わない。
- SELECT MUSIC画面（`music.py`の`MusicSelectScreen`）にも同じ`Backspace`/ゲームパッド長押しを追加：レース中でなくても対応するため、選択を強制的にPIXEL BREEZEへ戻し試聴を再生し直す（画面遷移はしない——そもそもこの画面にいる時点で戻る先は無いため）。
- `MusicPlayer.stop()`（新規）：`pygame.mixer.music.stop()`の安全なラッパー。
- ゴール後/タイムアップ後のヒント表示を`"score {n}  -  R restart  -  BACKSPACE reset"`に更新し、Reset操作が画面上でも分かるようにした。

これにより、SELECT MUSIC画面・レース中・ゴール後・タイムアップ後・リスタート待ち（＝ゴール後/タイムアップ後と同一の状態）のいずれからも、同じ`Backspace`/ゲームパッド長押しでResetできる。

### 検証結果

- `pytest`：既存93件＋新規22件（`tests/test_input_reset.py`新規5件、`tests/test_game.py`13件、`tests/test_music.py`4件）、計115件すべて成功
- `python main.py --race --test-frames 60`：ヘッドレスでクラッシュなく正常終了
- `config.json`は本作業でも一切変更していないことを`git diff`で確認済み（`save_config`が呼ばれていないことをテストでも確認）
- レース中からのBackspaceで、SELECT MUSICへ戻り、かつ新しいレースが正常に開始できることを`game.run()`レベルの結合テストで確認
- Restart（`R`）とReset（`Backspace`）が独立した動作であること（RestartはBGM/`_outcome`に影響しない）を確認
- 5回連続でResetしてもクラッシュしないことを確認
- 実際のゲームパッドが接続されていない（このセッション・CI含む）環境でも、`CONTROLLERBUTTONDOWN`/`UP`イベントを直接生成してテストすることで、長押し判定ロジック自体は検証済み

### 実機で確認してほしいこと

1. **実際のゲームパッドでのSelect/Backボタンの動作確認**：使用予定のコントローラーが実際にXbox/PS系としてSDLに認識され、`CONTROLLER_BUTTON_BACK`に対応するボタン（多くの場合「Select」「Back」「View」等の表記）で約1秒長押しするとResetが発火するか。認識されない機種の場合は生の`pygame.joystick`ボタン番号での個別対応が必要になる可能性がある。
2. 長押しの1秒という時間が、展示会での誤操作防止として適切な長さか（短すぎる/長すぎると感じたら`input_reset.py`の`RESET_HOLD_MS`で調整可能）。
3. `Backspace`キーでの即時Resetが、SELECT MUSIC画面・レース中・ゴール後・タイムアップ後のいずれからも違和感なく機能するか。
4. Reset時の「RESET」表示（150ms）が、左右の目でズレなく一つに見えるか、短すぎて見えない／長すぎてテンポを損なう、と感じないか。
5. SELECT MUSIC画面でBackspace/ゲームパッド長押しをした際、PIXEL BREEZEへ戻って試聴が正しく再生され直すか。

## 2026-09-04（3回目）: RESET機能の実機確認OK、SEをさらに強調

Reset機能（Backspace/ゲームパッド長押し）は実機で問題なく動作したとの報告。一方、「エンジン音・タイヤ軋み音がやはりほとんど実感できない」と、2026-09-04（2回目）のSE音量修正後も改善が不十分であるとのフィードバックを受けた。

### 対応

同日2回目の修正（`ENGINE_VOLUME`/`TIRE_SCREECH_VOLUME`を`0.32/0.45→0.8/0.8`、ソフトクリップ追加）で実効RMSは2.7〜4.4倍になっていたが、まだ不足と判断し、同じ手法（音量＋ソフトクリップ強度）でさらに踏み込んだ：

- `ENGINE_VOLUME`：`0.8→1.0`（`pygame.mixer.Sound.set_volume`が受け付ける上限で、これ以上の音量ヘッドルームは無い）
- `ENGINE_SATURATION_DRIVE`：`2.5→5.0`（tanhソフトクリップをより強くかけ、中間振幅のサンプルをさらに天井付近まで持ち上げる）
- `TIRE_SCREECH_VOLUME`：`0.8→1.0`（同上、上限）
- `TIRE_SCREECH_SATURATION_DRIVE`：`1.6→2.4`（エンジンより弱めのドライブは維持——ノイズ系の音はドライブを上げすぎると「キーキー」という質感が失われ、ただの潰れたノイズ（static）になりやすいため）

波形自体（周波数レンジ、倍音構成、タイヤのノイズ/トーン配合）は変更していない。音量・ソフトクリップ強度のみを再調整した。

### 検証結果（数値シミュレーション、`docs`の前回エントリと同じ手法）

- エンジン音の実効RMS：0.525 → **0.808**（約1.54倍、+約3.8dB）。ピークは0.996で1.0未満に収まり、ラップアラウンド的なクリップ・破裂音のリスクなし
- タイヤ軋み音の実効RMS：0.319 → **0.483**（約1.51倍、+約3.6dB）。ピークは0.9のまま
- 当初（2026-09-03、初回実装時）からの累積では、エンジン音の実効RMSは約0.119→0.808（約**6.8倍**）
- `pytest`：既存115件全て成功（波形の値レンジ・ループの継ぎ目なし等を検証する既存テストは、ドライブ強化後も条件を満たすことを確認）
- `python main.py --race --test-frames 60`：クラッシュなく正常終了
- `config.json`は本作業でも一切変更していないことを`git diff`で確認済み

### 実機で確認してほしいこと

1. エンジン音・タイヤ軋み音が今度こそはっきり感じられるか
2. 今回のドライブ強化（特にエンジン音の`ENGINE_SATURATION_DRIVE=5.0`）で、音が「迫力が出た」と感じるか、それとも「歪んで聞き苦しい」と感じるか——tanhソフトクリップは原理上ハードクリップより滑らかだが、今回はかなり強めにかけているため、実際に聞いてみないと判断できない
3. まだ物足りない場合、これ以上`*_VOLUME`側では上げられない（上限1.0）ため、次の一手は波形自体の見直し（倍音構成の変更、周波数レンジの調整、エンジン音とBGMの周波数帯域が被っていないかの確認等）になる見込み。あるいは展示会場のスピーカー/ヘッドホン自体の低域再生能力（エンジン音の基音は70〜240Hz）を疑う余地もある

## 2026-09-04（4回目）: エンジン音が「音量」ではなく「そもそも鳴っていない」可能性が判明

3回目の音量・ドライブ強化（`ENGINE_VOLUME`/`TIRE_SCREECH_VOLUME`を1.0に、ドライブを5.0/2.4に）を報告した直後、ユーザーから「まだエンジン音が出ているように、感じないですね」との追加フィードバック。ここまでの2回の修正（音量・ソフトクリップ）はいずれも「鳴ってはいるが小さい」という前提で対応していたが、この表現（「出ているように感じない」）はその前提そのものが誤っている可能性を示唆していた。

### 原因調査（ユーザーへの質問で切り分け）

実際に音を聞くことができないため、`D`キーのデバッグオーバーレイで`engine: on/unavailable`の表示を確認してもらうようユーザーに質問した。結果は**「`engine: unavailable`、またはbucketの数値が変化しない」**——つまり、これまでの音量・ソフトクリップの調整はそもそも実行されていなかった（`EngineSound.__init__`が`available=False`のまま早期returnしていた）可能性が高いことが判明した。ヘッドレス環境（SDLダミードライバ）では`EngineSound.available`は常に`True`になるため、このクラスの不具合はこれまでのテストでは検出できていなかった。

考えられる原因を2つ特定した：

1. **numpyが実機のvenvにインストールされていない可能性。** `numpy`は`sfx.py`実装時（2026-09-03）に`requirements.txt`へ追加された比較的新しい依存で、実機側で`pip install -r requirements.txt`が再実行されていなければ、`import numpy`が失敗し`sfx.py`が`np = None`にフォールバックする。この場合`EngineSound.__init__`は`fmt = _mixer_format() if np is not None else None`の時点で`None`になり、即座に`available=False`で終了する。BGMは`music.py`が`numpy`に依存していないため影響を受けず、実際「BGMは動いている」との報告と矛盾しない。**最も単純で可能性が高い原因。**
2. **実機のオーディオデバイスがミキサーを想定外のチャンネル数で初期化している可能性。** このプロジェクトはこれまで`pygame.mixer.init()`を明示的に呼んでおらず、`pygame.init()`によるミキサーの自動初期化に任せていた。`sfx.py`の`_mixer_format()`はチャンネル数が1か2でなければ`None`を返す設計になっており、実機のHDMIオーディオ経路などが例えば5.1chのような値を報告した場合、同様に`EngineSound`/`TireScreech`とも`available=False`になる。ヘッドレスのSDLダミードライバは常に無難な値を返すため、この種の不具合もこれまでのテストでは再現しなかった。

### 対応

原因を一発で確定させることはできないため（実機で確認できないセッションの限界）、両方の原因に対処しつつ、**次回に一目で原因が分かる診断機能**を追加する方針にした：

1. **ミキサーを明示的に既知の設定でpre_init：** `game.py`のモジュールレベル`run()`冒頭で`pygame.mixer.pre_init(frequency=44100, size=-16, channels=2, buffer=512)`を`pygame.init()`より前に呼び、ミキサーが実機のオーディオデバイスとの自動ネゴシエーションに任せず、常にchannels=2で初期化されるようにした。`pygame.init()`後に`pygame.mixer.get_init()`が失敗していた場合の保険として、明示的な`pygame.mixer.init(...)`の再試行も追加。
2. **`sfx.py`に`unavailable_reason`診断を追加：** `EngineSound`/`TireScreech`が`available=False`になった具体的な理由（`"numpy not installed"` / `"pygame.mixer not initialized..."` / `"unsupported mixer channel count (N, expected 1 or 2)"` / `"sound creation failed: ..."`）を新規属性`unavailable_reason`に記録するようにした。`_mixer_format()`は`(fmt, reason)`のタプルを返す設計に変更。
3. **`D`キーのデバッグオーバーレイに理由を表示：** `engine`/`tire_screech`が`unavailable`のとき、`"engine unavailable: <reason>"`の行を追加表示するようにした。次回実機で確認する際、これを見れば「numpyが無いのか」「ミキサーのチャンネル数の問題か」「他の例外か」が一目で分かる。

### 検証結果

- `pytest`：既存115件＋新規12件（`tests/test_sfx.py`10件、`tests/test_game.py`1件、既存2件に`unavailable_reason`のアサーション追加）、計121件すべて成功
- `python main.py --race --test-frames 60`：クラッシュなく正常終了
- SDLダミードライバ環境で`pygame.mixer.pre_init(...)` → `pygame.init()`後に`pygame.mixer.get_init()`が`(44100, -16, 2)`になることを確認（headless環境でも既存の挙動を壊していない）
- `config.json`は本作業でも一切変更していないことを`git diff`で確認済み

### 実機で確認してほしいこと（最優先）

1. **まず`pip install -r requirements.txt`を実機のvenvで再実行し、`numpy`が入っているか確認してほしい**（`pip show numpy`または`python -c "import numpy"`でエラーが出ないか）。もし入っていなければ、これが今回の根本原因だった可能性が高い。
2. その上で`python main.py --race`を実行し、レース中に`D`キーでデバッグオーバーレイを表示。`engine: on bucket=X/15`と表示され、加速するとbucketの数値が増えるか確認してほしい。
3. もし`engine: unavailable: <理由>`と表示された場合、その理由の文字列をそのまま教えてほしい——原因を一意に特定できる。
4. `engine: on`と表示されているのに音が聞こえない場合は、今度こそ純粋な「音量/知覚」の問題（周波数帯域がスピーカーで再生しづらい等）である可能性が高く、その場合は波形（周波数レンジ・倍音構成）自体の見直しに進む。

## 2026-09-04（5回目）: エンジン音が実際に鳴っていることを実機で確認、ただし音量が過大——初回のレベルへロールバック

前回追加した診断（`unavailable_reason`表示＋`pygame.mixer.pre_init`）の後、ユーザーから「音がでました。ただちょっとうるさいですねｗ」との報告。エンジン音（および恐らくタイヤ軋み音も同様に）が実機で初めて正しくトリガーされたことが確認できた一方、**音が大きすぎる**という新しいフィードバックになった。

### 状況の整理

これまでの経緯を振り返ると、以下の通り「音量調整」が実は的外れだった可能性が高い：

- 2026-09-04（1回目のフィードバック、このセッション開始前）：「ほとんど聞こえない」→ 音量0.32/0.45→0.8/0.8＋ソフトクリップに引き上げ（1回目の対応）
- 同日（2回目のフィードバック）：「まだほとんど実感できない」→ 音量0.8/0.8→1.0/1.0、ドライブ2.5/1.6→5.0/2.4にさらに引き上げ（2回目の対応）
- 同日（3回目のフィードバック）：「まだ出ているように感じない」→ 実機で`D`キーオーバーレイを確認してもらったところ`unavailable`と判明。**この時点までの音量調整は一度も実際には適用されていなかった**（`EngineSound`が有効化に失敗し早期returnしていたため）
- ミキサーの`pre_init`と`unavailable_reason`診断を追加（4回目の対応）
- 今回：**音が実際に鳴るようになり、その結果「うるさい」と分かった**

つまり、1回目・2回目の「まだ聞こえない」というフィードバックは、実際には「音量が足りない」のではなく「（同じ理由で）そもそも鳴っていなかった」可能性が高く、音量を最大（1.0）・ドライブを最強（5.0/2.4）まで引き上げたのは、鳴っていない音に対して行った的外れな調整だった可能性が高い。

### 対応

`ENGINE_VOLUME`/`ENGINE_SATURATION_DRIVE`/`TIRE_SCREECH_VOLUME`/`TIRE_SCREECH_SATURATION_DRIVE`を、2回目の引き上げ前の値（1回目の対応時の値）へロールバックした：

| 定数 | 2回目（今回まで） | 5回目（今回、ロールバック後） |
|---|---|---|
| `ENGINE_VOLUME` | 1.0 | **0.8** |
| `ENGINE_SATURATION_DRIVE` | 5.0 | **2.5** |
| `TIRE_SCREECH_VOLUME` | 1.0 | **0.8** |
| `TIRE_SCREECH_SATURATION_DRIVE` | 2.4 | **1.6** |

この値は「1回目の音量修正」として一度実装済みだった、根拠のある値であり、今回はゼロから見積もり直したわけではない。今回初めて「実際に鳴っている状態」でユーザーに聞いてもらえることになるため、この値がちょうど良いかはまだ未確定——次回さらに聞いた上での微調整が前提。

### 検証結果

- 数値シミュレーション：エンジン音の実効RMSが0.808→0.525、タイヤ軋み音が0.483→0.319に戻ったことを確認（いずれも1回目修正時と同じ値）
- `pytest`：既存121件（定数の妥当性を直接アサートするテストは無いため、変更なしで全て成功）
- `python main.py --race --test-frames 60`：クラッシュなく正常終了
- `config.json`は本作業でも一切変更していないことを`git diff`で確認済み

### 実機で確認してほしいこと

1. 今回のロールバックで、エンジン音・タイヤ軋み音の音量感がちょうど良いか、まだうるさいか、あるいは今度は物足りないか
2. BGM・エンジン音・タイヤ軋み音、3つを同時に聞いたときのバランス
3. 「うるさい」がエンジン音・タイヤ軋み音のどちらだったか（または両方か）——もしどちらか一方だけが気になる場合、次回はその音だけを個別に下げられる

## 2026-09-04（6回目）: エンジン音の波形・RPMモデルを全面再設計（3プリセット）

Reset機能・SEトリガー修正・音量ロールバックの後、ユーザーから追加フィードバック：「止まっている時のエンジン音はいいですね。タイヤの軋み音は実感できないです。BGMに比べてかなりまだ大きいですね。エンジン音が再生されることは確認できました。ただし、現在の音はイメージしているエンジン音とは異なります」。走行中のエンジン音が「イメージと異なる」との指摘を受け、単純な音量・歪み調整ではなく、**波形構成とRPM（回転数）モデルそのものを見直す**方針で対応した。タイヤ軋み音・BGM・入力・レース進行・キャリブレーション設定はこの対応の対象外（ユーザーからも明示的に変更しないよう指示あり）。

### 現状分析（実装前にユーザーへ報告した内容）

- **波形**：純粋なサイン波の倍音を1〜5倍音（振幅1.0/0.5/0.3/0.15/0.08）で単純に加算合成していただけ。倍音の量が少なく角が丸いため、締まった「唸り」ではなく間延びした音になりやすい
- **ピッチ制御**：「回転数」という概念が無く、`speed_frac`（車速の正規化値）を70Hz〜240Hzへ**直接・線形にマッピング**。アクセルを踏むと即座に音程が上がるだけで、回転数が「追いついてくる」感覚や、離した時に「遅れて下がる」挙動が無かった
- **音の押し出し**：太さ・音圧をほぼ**tanhソフトクリップ（歪み）の強さだけ**で作ろうとしていたため、音量を上げるほど「潰れたノイズ」に近づく設計だった
- **最高速での質感**：倍音の振幅比率は変わらずピッチだけが上がるため、最高速では基音が細く高い音だけが残りやすく、「ブザー／警報音」寄りに聞こえる構造的な問題があった

この分析に基づき、「止まっている時の感覚は近い（→アイドル周波数はほぼ現状維持）」「走行中に違和感がある（→回転数の挙動と倍音構成を作り直す）」という指摘と整合する形で再設計した。

### 対応：RPMモデル

`EngineSound`に内部状態`engine_rpm`（0〜1、車速そのものではない）を追加。`speed_frac`を目標値として、**上昇は速く（`ENGINE_RPM_RISE_RATE=3.5`/秒）、下降は遅く（`ENGINE_RPM_FALL_RATE=1.2`/秒）**という非対称な補間で追従させる：

```python
rate = ENGINE_RPM_RISE_RATE if target_rpm > engine_rpm else ENGINE_RPM_FALL_RATE
engine_rpm += (target_rpm - engine_rpm) * min(1.0, rate * dt)
```

- 「アクセルを押すと回転数が上がっていくことが明確に分かる」→ 上昇レートを速めに設定
- 「アクセルを離すと、少し遅れて回転数が下がる」→ 下降レートを遅めに設定
- 「衝突・急減速：回転数も遅れて下がる」→ 衝突による急な速度低下も同じ「目標値が急に下がる」ケースとして扱われるため、**追加の実装なしで**同じ非対称補間でカバーされる
- フレームごとの音程の不自然な揺れを防ぐため、`dt`（フレーム時間）ベースの補間を採用——このため`EngineSound.update()`のシグネチャに`dt`引数を追加（`game.py`の呼び出し側・既存テストも合わせて修正）

### 対応：波形構成（3レイヤー）

単一の加算合成ではなく、以下の3レイヤーを混ぜる方式に変更：

1. **低い基本波（fundamental）**：三角波系（奇数倍音のみ、振幅1/k²で減衰、正負交互）を`_harmonic_series_wave()`で生成。角が丸く「太さ」を作る中心的な成分で、常に**混合比率の下限`ENGINE_FUNDAMENTAL_FLOOR=0.45`を下回らない**——「最高速では高音だけにならず、低いエンジン成分も残る」を構造的に保証
2. **中域の倍音（growl）**：鋸歯状波または矩形波（全倍音または奇数倍音、振幅1/kで減衰）を、**基本波と同じ周波数**（別の音程ではなく同じ音の「明るさ」を足す）で混合。混合比率はRPMに応じて`growl_weight_idle`（アイドル時）から`growl_weight_max`（最高回転時）へ線形に増加——「速度が上がるにつれて、音程だけでなく倍音と勢いも増す」を実現
3. **ごく小さなノイズ成分**：ホワイトノイズを5タップの移動平均で軽く低域通過させた「機械的なざらつき」テクスチャ。タイヤ軋み音の鋭いノイズとは別物で、常に控えめな比率（`noise_weight`、5〜7%程度）

3つの比率は`(fundamental, growl, noise)`の合計が1になるよう都度正規化。**現在の強いSaturation Driveには頼らない**方針の通り、`saturation_drive`は1.15〜1.2程度（旧実装の2.5〜5.0から大幅に低下）に抑え、あくまでピーク超過を防ぐ安全弁として使うのみ。

`_harmonic_series_wave(waveform, freq, t, harmonics)`は、生の不連続な矩形波/鋸歯状波（サンプリングレートいっぱいまで倍音を含む、明るすぎる/エイリアシングしやすい）ではなく、**指定した本数の正確なサイン波倍音を合成した band-limited近似**として実装——`harmonics`が明るさ・ざらつき感の直接的な調整ノブになる（多いほど明るい/バジー、少ないほど丸い/シンプル）。

### 対応：3プリセット（A/B/C）

音の好みは実機で聞かないと判断できないため、単一案に決め打ちせず3種類を実装し、`E`キーで走行中に切り替えられるようにした：

| プリセット | 特徴 | 基本波 | 中域倍音層 |
|---|---|---|---|
| **A. LOW RUMBLE** | 低く太い音を重視、明るさの変化は控えめ | 三角波・7倍音 | 鋸歯状波、最大22% |
| **B. ARCADE ENGINE** | バランス型、1990年代アーケードらしい唸り | 三角波・7倍音 | 鋸歯状波、最大40% |
| **C. CHIP ENGINE** | よりシンプル/「カクカク」した、Virtual Boy風だが単純なブザーにはしない | 矩形波・4倍音 | 矩形波、最大38% |

デフォルトは**ARCADE ENGINE**。`E`キーで切替、画面に一時的にプリセット名を表示（視差ゼロ、1.2秒）。`D`キーのデバッグオーバーレイにも現在のプリセット名と`engine_rpm`の値を追加表示。プリセット切替時は現在のRPMバケットを新プリセットの音で即座に再トリガー（クロスフェード）するようにし、切替が即座に聞こえるようにした。

### 対応：SFX TESTツール（新規`sfx_test.py`）

- `python main.py --sfx-test`：対話型の比較画面。`A`/`B`/`C`でプリセット、`1`〜`4`でアイドル/低速/中速/高速を切り替え。`EngineSound`の実際の`update()`/RPM補間ロジックがそのまま毎フレーム動くため、速度レベルを切り替えると実際の「回転数が遅れて上下する」挙動もその場で確認できる
- `python sfx_test.py --export-wav <dir>`：3プリセット×4速度＝12個の確認用WAVファイル（`debug_engine_<preset>_<speed>.wav`、約1.6秒）をオフライン合成して書き出す。ライブのミキサー不要（純粋にnumpyで波形生成しWAV書き出しのみ）。**Gitにはコミットしない**——`.gitignore`に`debug_wav/`を追加済み、README側にも明記
- `main.py`に`--sfx-test`フラグを追加（既存の`--race`/`--stereo-test`と同じパターン）

### 検証結果

- `pytest`：既存121件から大幅増、計**153件**すべて成功（`tests/test_sfx.py`を新API対応に全面改訂、`tests/test_sfx_test.py`新規11件、`tests/test_game.py`にプリセット切替関連4件を追加）
- 波形の数値検証：`_engine_wave`が全プリセット・全RPM段階でピーク1.0以内・非無音であることを確認
- RPM補間の非対称性（上昇1秒でほぼ目標到達、下降1秒では途中までしか戻らない）を数値シミュレーションで確認
- `render_engine_preview()`で12個のWAVを実際に書き出し、簡易なゼロ交差法によるピッチ推定で「アイドル→高速で周波数が上昇する」ことを3プリセット全てで確認（例：ARCADE ENGINEで約108Hz→177Hz）。ピークは全サンプルで1.0未満（0.86〜0.98）に収まりクリップなし。CHIP ENGINEはfundamental/growl両方が矩形波系のためRMSが他の2プリセットより高め（0.83 vs 0.60前後）——`volume=0.75`でやや低めに設定して部分的に補正済みだが、実際に聞くと3プリセット間で音量差を感じる可能性がある点は要注意
- `python main.py --race --test-frames 120`、`python main.py --sfx-test --test-frames 30`：いずれもヘッドレスでクラッシュなく正常終了
- `config.json`、タイヤ軋み音（`TireScreech`・`TIRE_SCREECH_*`定数）、BGM、入力、キャリブレーションのいずれも本対応で変更していないことを`git diff`で確認済み

### 実機で確認してほしいこと（最優先）

1. **3プリセット（`E`キーで切替）のうち、どれが最もイメージに近いか。** 「1990年代アーケード／携帯ゲーム機のスポーツカーのエンジン音」として、低く太い唸り・回転数の分かりやすい変化・歪みすぎない質感になっているか
2. アクセルを踏んだ時の「回転が上がっていく」感覚、離した時の「少し遅れて下がる」感覚が自然か（`ENGINE_RPM_RISE_RATE`/`ENGINE_RPM_FALL_RATE`で調整可能）
3. 最高速でも低いエンジン成分が残っているか、高音だけのブザーっぽさが無くなったか
4. CHIP ENGINEが他の2つより大きく聞こえないか（上記の通りRMSが高めなので、体感で気になる場合は`CHIP ENGINE`の`volume`をさらに下げる）
5. `python main.py --sfx-test`での比較、可能であれば`python sfx_test.py --export-wav <dir>`で書き出したWAVをその場で（あるいは別途）聞いての判断も歓迎——ただしこれらのWAVファイル自体はコミットしないよう運用上も注意

## 2026-09-04（7回目）: エンジン音再設計を実機評価、ARCADE ENGINE採用・音量微調整

3プリセットを実機で聞き比べてもらったところ、ユーザーから「エンジン音は、最初が一番イメージにちかいですね。ブザーっぽさも消えました。BGMに比べてまだエンジン音が大きい気がします。アクセルON/OFFの変化も自然になりました」とのフィードバック。

### 評価結果

- 「最初」＝デフォルトプリセット`ARCADE ENGINE`（`ENGINE_DEFAULT_PRESET`）が最もイメージに近いと確認——LOW RUMBLE/CHIP ENGINEとの比較で最有力候補
- 波形再設計（3レイヤー構成・ソフトクリップ抑制）の狙い通り、「ブザーっぽさ」が解消したことを確認
- RPMモデル（非対称補間）による「アクセルON/OFFの変化が自然」なことも確認——今回追加した2つの主要な設計要素（波形・RPM挙動）はいずれも意図通りに実機で評価された
- 残る課題は「BGMに比べてまだ大きい」という**音量バランスのみ**（波形・キャラクターの再修正は不要）

### 対応

`ARCADE ENGINE`プリセットの`volume`のみを`0.8→0.6`に引き下げ。波形構成（`fundamental_wave`/`growl_wave`/各倍音数/`saturation_drive`）は「イメージに近い」「ブザーっぽさが消えた」と評価された内容のため一切変更していない。`LOW RUMBLE`/`CHIP ENGINE`の2プリセットは今回未評価のため、`volume`含め変更せず維持。

### 検証結果

- 数値シミュレーション：`ARCADE ENGINE`の実効RMSが0.47〜0.49（`volume=0.8`時、RPM 0〜1で微減）→0.35〜0.37（`volume=0.6`）に低下（約-2.5dB）。BGMの`BGM_VOLUME=0.65`（`music.py`）と比べても相対的に控えめな値になった
- `pytest`：既存153件全て成功（`EnginePreset`の値のみの変更のため、テストコード自体は無変更）
- `python main.py --race --test-frames 60`：クラッシュなく正常終了
- `config.json`・タイヤ軋み音・BGM・入力・キャリブレーションは本対応でも変更なし

### 実機で確認してほしいこと

1. 音量を下げた`ARCADE ENGINE`が、BGMとのバランスとして「ちょうど良い」か、まだ大きいか、あるいは今度は物足りないか
2. （引き続き）タイヤ軋み音の「実感できない」問題——今回もスコープ外のまま

## 2026-09-04（8回目）: ARCADE ENGINEの音量をさらに下げる

7回目の音量調整（`volume` 0.8→0.6）を報告したところ、ユーザーから「BGMに対して、まだもっと小さくてもよいです」との追加フィードバック。深刻な指摘ではなく軽微な微調整の依頼と判断し、同じ比率感（前回は0.8→0.6で約25%減）でもう一段階下げた。

### 対応

`ARCADE ENGINE`の`volume`のみ`0.6→0.4`に変更（波形・ドライブは今回も無変更——7回目までに「イメージに近い」と評価済みの内容のため）。

### 検証結果

- 数値シミュレーション：実効RMSが約0.35〜0.37→約0.24〜0.25に低下。BGMの`BGM_VOLUME=0.65`より明確に控えめな値
- `pytest`：既存153件全て成功
- `python main.py --race --test-frames 60`：クラッシュなく正常終了
- `config.json`・タイヤ軋み音・BGM・入力・キャリブレーションは本対応でも変更なし

### 実機で確認してほしいこと

1. この音量でBGMとのバランスがちょうど良いか、まだ大きいか、今度は小さすぎるか

## 2026-09-04（9回目）: タイヤ軋み音の発火頻度を大幅に引き上げ

エンジン音の音量調整（`volume=0.4`）が「これでいいですね」と確定した直後、「タイヤの軋む音にいきましょう」との指示。以前から「タイヤの軋み音は実感できない」という指摘があったため、対応した。

### 現状分析（実装前にユーザーへ報告）

ヘッドレス環境でフルスロットル・ノーステアのフルラップをシミュレーションし、実際の発火状況を数値で確認した：

- `abs(current_curve) * speed_frac`（既存の遠心力ドリフト補正の元値を流用したトリガー基準）が旧閾値`TIRE_SCREECH_THRESHOLD=0.15`を超えるのは全体の3.6%
- しかし実際に「鳴っている」区間は**コース全体でたった2回、各1.3〜1.4秒（合計約2.7秒）**——1周約74秒に対して極めて希少
- 波形自体の実効音量（RMS、`TIRE_SCREECH_VOLUME=0.8`時で約0.32）は、直前に「ちょうど良い」と評価されたARCADE ENGINE（実効RMS約0.24〜0.25）より**むしろ大きい**——単純な音量不足が主因ではないと判断

この分析結果をユーザーに報告し、対応方針を確認したところ「閾値を下げて、もっと頻繁に鳴らす」を選択。さらに「ステアリング切るたびになるくらいでもいいですよ」という追加指示があった。

### 対応

候補となる閾値ごとに同じフルラップシミュレーションで発火頻度を計測し、「ステアリングを切るたびに近い」体感になる値を選定：

| 閾値 | 発火区間の割合 | エピソード数 | 合計発火時間 |
|---|---|---|---|
| 0.15（旧） | 3.6% | 2 | 2.7秒 |
| 0.10 | 10.4% | 14 | 7.7秒 |
| 0.08 | 24.5% | 34 | 18.1秒 |
| 0.06 | 34.8% | 37 | 25.8秒 |
| **0.05（新）** | **39.4%** | **36** | **29.2秒** |
| 0.04 | 43.7% | 35 | 32.4秒 |

`TIRE_SCREECH_THRESHOLD`を`0.15→0.05`に変更。約1/3の閾値まで下げたことで、実質的にカーブを曲がるたびに鳴る頻度になった。音量（`TIRE_SCREECH_VOLUME=0.8`）・波形（`_tire_screech_wave`）・ドライブ（`TIRE_SCREECH_SATURATION_DRIVE=1.6`）は今回変更していない（頻度の問題であり、音量・音色の問題ではないと分析したため）。

### 検証結果

- `pytest`：既存153件＋新規1件（`tests/test_game.py`の`test_tire_screech_triggers_frequently_over_a_full_race`——フルラップシミュレーションで発火区間が20%を超えることを検証、旧実装なら3.6%で確実に落ちる回帰テスト）、計**154件**すべて成功
- `python main.py --race --test-frames 60`：クラッシュなく正常終了
- `config.json`・エンジン音・BGM・入力・キャリブレーションは本対応で変更なし

### 実機で確認してほしいこと

1. ステアリングを切るたびに鳴る、という体感に近くなったか
2. 発火頻度が高くなったことで、逆にうるさく感じないか（気になる場合は`TIRE_SCREECH_THRESHOLD`をやや上げて調整可能）
3. 同じ音（固定のノイズ+トーン波形）が頻繁に繰り返されることで単調に感じないか——気になるようであれば、エンジン音のように波形のバリエーションを増やす対応も検討可能

## 2026-09-04（10回目）: タイヤ軋み音を3プリセット化

タイヤ軋み音の新しい発火頻度（`TIRE_SCREECH_THRESHOLD=0.05`）を報告したところ、ユーザーから「いや、このくらいでいいですよ。音も何パータンかつくって、どれがいいかテストしたいです」との返信。頻度は確定、次は**音そのもの**（波形・音色）のバリエーションをテストしたい、という要望に対応した。

### 対応方針

エンジン音のとき（RPMモデル＋3レイヤー波形＋A/B/C 3プリセット＋`E`キー切替＋SFX TESTツール）と同じ設計パターンを踏襲。タイヤ軋み音側にも`TireScreechPreset`データクラスを新設し、3種類の波形パターンを用意した：

| プリセット | 特徴 | ノイズ比率 | トーン中心周波数 | 長さ |
|---|---|---|---|---|
| **CLASSIC SQUEAL** | 従来実装をそのまま初期値としてプリセット化（変更なし） | 0.5 | 1900Hz | 0.35秒 |
| **GRIP SLIDE** | ノイズ主体・低めのピッチ、「キーキー」よりゴムが擦れる「ガリガリ」感 | 0.75 | 900Hz | 0.4秒 |
| **ARCADE CHIRP** | 短く明るいトーン主体、アーケードレーサー風の一瞬の「キュッ」というチャープ | 0.3 | 2600Hz | 0.22秒 |

閾値（`TIRE_SCREECH_THRESHOLD`）とフェード時間（`TIRE_SCREECH_FADE_MS`）はゲームプレイ上の「いつ鳴るか」を制御する値としてグローバル共有のまま維持。「どう鳴るか」（音量・波形・ドライブ・長さ）のみプリセットごとの値に分離した（`TIRE_SCREECH_VOLUME`/`TIRE_SCREECH_SATURATION_DRIVE`/`TIRE_SCREECH_SECONDS`という単一モジュール定数は廃止）。

### 実装内容

- `sfx.py`：`TireScreechPreset`データクラス、`TIRE_SCREECH_PRESETS`辞書、`TIRE_SCREECH_PRESET_ORDER`、`TIRE_SCREECH_DEFAULT_PRESET`（`CLASSIC SQUEAL`）を追加。`_tire_screech_wave()`は`preset`引数を受け取るように変更。`TireScreech`クラスは3プリセット全てを起動時に事前レンダリングし、`set_preset()`で即座に切替可能に（エンジンと異なり、タイヤ軋み音は短いワンショットのため、切替時に強制的な即時再トリガーはしない——再生中のクリップは自然に終わらせ、次のトリガーから新プリセットを使う設計）。テスト用に`play_now()`（閾値を無視して即座に再生）も追加
- `game.py`：`T`キーでタイヤ軋み音のプリセットを`E`キーと同様に循環切替、画面に「TIRE: プリセット名」を一時表示。エンジンとタイヤで共通のプリセット切替ロジックを`_cycle_preset()`ヘルパーに統合（`_cycle_engine_preset`/`_cycle_tire_preset`から呼び出し）。`D`キーのデバッグオーバーレイにも`preset=...(T)`を追加表示
- `sfx_test.py`：既存のエンジン比較画面にタイヤ軋み音のプリセット選択（`X`/`Y`/`Z`キー）と即時再生（`SPACE`キー）を追加。`--export-wav`は既存12個のエンジンWAVに加え、タイヤ軋み音3プリセット分（`debug_tire_<preset>.wav`）も書き出すように拡張（引き続きGitにはコミットしない）

### 検証結果

- `pytest`：既存154件→**172件**、全て成功（`tests/test_sfx.py`にプリセット定義・波形生成・`TireScreech`の多プリセット対応・`set_preset`/`play_now`のテストを追加、`tests/test_game.py`に`T`キー関連4件＋エンジン/タイヤのフラッシュ表示が競合しないことの確認1件、`tests/test_sfx_test.py`にタイヤ軋み音WAV書き出し・UI操作のテストを追加）
- `python main.py --race --test-frames 120`、`python main.py --sfx-test --test-frames 30`：いずれもヘッドレスでクラッシュなく正常終了
- 実際に`sfx_test.py --export-wav`で3プリセット分のWAVを書き出し、数値検証（RMS 0.37〜0.44、ピークは全て0.9で統一——歪み・クリップなし）で3者が明確に異なる特性を持つことを確認
- `config.json`・エンジン音・BGM・入力・キャリブレーションは本対応で変更なし

### 実機で確認してほしいこと

1. `python main.py --race`中に`T`キーで3プリセット（CLASSIC SQUEAL / GRIP SLIDE / ARCADE CHIRP）を聞き比べ、どれが最もイメージに近いか
2. `python main.py --sfx-test`でも比較可能（`X`/`Y`/`Z`キーで選択、`SPACE`キーで即再生）——エンジン音同様、実際に運転せずに聞き比べられる
3. どれか一つに決まったら、そのプリセットをデフォルトにする対応（`TIRE_SCREECH_DEFAULT_PRESET`変更）を次回実施

## 2026-09-05（11回目）: タイヤ軋み音はCLASSIC SQUEALに確定、Xboxコントローラーを正式サポート

タイヤ軋み音の3プリセットを実機で聞き比べてもらった結果、「clasicが一番近いですね」との回答——`CLASSIC SQUEAL`（デフォルト、従来実装をそのまま初期値としてプリセット化したもの）で確定。追加のコード変更は不要（既にデフォルトのため）。

続けてユーザーから、「XboxコントローラーはWindowsの`joy.cpl`で正常に認識され、ボタンとスティックも反応しているが、現在のゲームでは操作できない。外部のキーマッピングソフトは使用せず、Pygame側でXboxコントローラーへ正式対応してほしい」との依頼。起動時の詳細診断ログ（コントローラー数・名前・GUID・軸数・ボタン数・ハット数・SDL GameController認識の有無）、SELECT MUSIC画面とレース中の操作割り当て、左スティックのデッドゾーン、`pygame._sdl2.controller`によるSDL標準GameControllerマッピングの使用（ボタン番号の決め打ち回避）が明示的に指定された。

### 調査結果（実装前にユーザーへ報告した内容）

リポジトリ全体を確認したところ、ゲームパッド関連のコードは`input_reset.py`（2026-09-04のRESET機能実装時に新規追加）のみで、その中身は**Select/Backボタン長押しでResetする機能だけ**だった：

- `open_connected_controllers()`：`pygame.joystick.Joystick`を開くのみ
- `GamepadResetHold`：`CONTROLLERBUTTONDOWN`/`UP`イベントでSelect/Backの長押しを検出

**ステアリング・アクセル・ブレーキ・SELECT MUSIC画面のD-pad/スティック操作を読み取るコードは一切存在しなかった。** これが「Windowsでは認識されているのにゲームでは操作できない」の直接の原因——OSレベルの認識（`joy.cpl`）とゲーム内での実際の入力読み取りは別物であり、後者が丸ごと未実装だった、という単純な原因だった。

### 対応：`pygame._sdl2.controller`の採用

`pygame`本体の`pygame.joystick`（ボタン番号が機種依存）ではなく、`pygame._sdl2.controller`モジュール（SDLの標準GameControllerマッピングを使用、`CONTROLLER_BUTTON_A`等の意味付きの定数でボタンを扱える）を採用。このモジュールのAPI（`Controller(index)`、`get_axis()`が`-32768`〜`32767`の生の整数を返す、`get_button()`が`bool`を返す、`attached()`で接続状態を確認できる等）は、この開発環境にインストール済みのpygame 2.6.1のスタブファイル（`.pyi`）と公式rstドキュメントから正確に確認した上で実装している（実機のコントローラーが無いため、動作は静的な仕様確認に基づく——実機での最終確認が必要、下記参照）。

### 対応：モジュールの再構成（`input_reset.py` → `gamepad.py`）

ゲームパッド機能がReset専用から「ゲームパッド全般の入力処理」へと役割が大きく広がったため、`input_reset.py`を`gamepad.py`へリネーム（`git mv`でリネーム履歴を保持）。参照元の`music.py`・`game.py`・テストを追従修正。

### 対応：起動時診断ログ

`gamepad.open_connected_controllers()`（`game.run()`から起動時に1回呼ばれる）が、検出した各ジョイスティックについて以下を標準出力へ出力する：

```
[gamepad] N controller(s) detected
[gamepad] #0: name='...' guid=... axes=... buttons=... hats=... sdl_game_controller=True/False
```

`sdl_game_controller`は`pygame._sdl2.controller.is_controller(index)`で判定。`False`の場合、そのデバイスはWindows側やこのログには表示されるが、SDLのマッピングDBに無いため本実装のどの操作にも反応しない（生のジョイスティックボタン番号へのフォールバックは意図的に実装していない——「ボタン番号を直接決め打ちしない構成に」という指示と矛盾するため）。ゲーム起動後に接続されたコントローラーも`CONTROLLERDEVICEADDED`イベントで同様に検出・ログ出力される。

### 対応：入力の実装

**SELECT MUSIC画面**（`music.py`）：
- D-pad左右：`CONTROLLERBUTTONDOWN`イベントで即座に曲切替（ボタンは連打しない限り自動的に単発なので、追加のデバウンス不要）
- Aボタン／STARTボタン：`CONTROLLERBUTTONDOWN`で選曲確定・ゲーム開始
- 左スティック左右：新規`gamepad.MenuStickNav`クラスで処理。デッドゾーン（`MENU_STICK_DEADZONE=0.5`、操舵用よりかなり粗い——「明確に倒したか」の二値判定のため）を超えた**最初の立ち上がり**で即座に発火し、倒し続けた場合は`MENU_STICK_REPEAT_MS=250`ms間隔で再発火する設計。「曲が高速で連続切り替えされないよう」という指示に対し、立ち上がり判定とリピート間隔の両方を実装した

**レース中**（`game.py`）：
- 左スティック左右：`gamepad.read_steer()`でアナログステアリング（デッドゾーン`STEER_DEADZONE=0.18`、指定範囲0.15〜0.20の中間値）
- D-pad左右：同じ`read_steer()`内でデジタルステアリングとして加算（スティックと併用可、最終的に±1にクランプ）
- Aボタン：`gamepad.read_accel()`でアクセル（キーボードの↑/Wと同等の入力として扱う、`Game.update()`のor条件に追加）
- Bボタン：`gamepad.read_brake()`でブレーキ（↓/Sと同等）
- STARTボタン：`Game._handle_controller_button_down()`（新規、`_handle_keydown`と同様のテスト可能な形に切り出し）で押下時に`_restart()`を呼ぶ。「既存のポーズ／スタート操作」という指示だが、レース中にポーズ機能自体が存在しないため、既存の「もう一度始める」操作＝Restart（`R`キーと同等）へマッピングした——この解釈で問題があれば別の割り当てに変更可能
- BACK/VIEWボタン長押し：既存の`GamepadResetHold`（2026-09-04実装、Reset機能）をそのまま利用、変更なし

`Game.update(dt, keys, gamepad=None)`のように`gamepad`引数を追加（デフォルト`None`）——既存のキーボードのみのテスト・呼び出し元は無変更で動作する。

### 対応：デッドゾーン・抜き差し・グレースフルデグレード

- `gamepad._axis()`/`_button()`：コントローラーが`None`、または読み取りが失敗した場合は`0.0`/`False`を返す（例外を投げない）
- `gamepad.get_primary_controller()`：呼び出しごとに`attached()`で接続状態を確認し、切断済みのコントローラーを内部リストから除去。抜いた瞬間から自動的にキーボードのみの状態へフォールバックし、抜き差し時に例外は発生しない
- SDLの生スティック軸範囲（`-32768`〜`32767`）を正規化する際、`-32768/32767`が`-1.0`をわずかに下回るため、`max(-1.0, min(1.0, ...))`で必ずクランプ

### 検証結果

- `pytest`：既存192件→**207件**、全て成功（`tests/test_gamepad.py`を全面刷新——診断ログ出力の確認、`read_steer`/`read_accel`/`read_brake`のデッドゾーン・D-pad・組み合わせ・極端値クランプ、`MenuStickNav`の立ち上がり判定・リピート間隔・方向転換時の即時発火、`get_primary_controller`の抜き差しシミュレーション、いずれもpygame公式スタブに準拠した最小限のフェイクController（`get_axis`/`get_button`/`attached`のみ実装）を使用。`tests/test_game.py`にA/B/D-pad/スティックでの加速・ブレーキ・ステアリング、STARTボタンでのRestart、`gamepad=None`後方互換性の確認を追加。`tests/test_music.py`にD-pad/スティック/A/STARTでのSELECT MUSIC操作を追加）
- `python main.py --race --test-frames 120`：ヘッドレスでクラッシュなく正常終了、起動ログに`[gamepad] 0 controller(s) detected`（この環境には実機コントローラーが無いため）を確認
- `python main.py --sfx-test`・`--stereo-test`・（デフォルトの）キャリブレーターも起動確認、いずれも正常
- `config.json`・BGM・エンジン音・タイヤ軋み音の波形自体は本対応で変更なし
- キーボード操作は全てのテスト・スモークテストで無変更のまま動作することを確認（`gamepad`引数はデフォルト`None`のため、既存の呼び出しは全て無修正で動く）

### 実機（Xboxコントローラー）でなければ確認できない項目

1. **`sdl_game_controller=True`と表示されるか。** Xboxコントローラーは通常SDLの標準マッピングDBに含まれているため`True`になるはずだが、この開発環境には実機が無いため確認できていない。`False`だった場合、生のジョイスティックボタン番号での個別対応が別途必要になる
2. 起動時ログの`name`/`guid`/`axes`/`buttons`/`hats`が実際の値として妥当か
3. 左スティックのデッドゾーン`0.18`が「中央付近で勝手に曲がらない」かつ「操作の遊びが大きすぎない」ちょうど良いバランスか
4. SELECT MUSIC画面でのスティック操作の立ち上がり判定・リピート間隔（`250ms`）が「連続切り替えしすぎず、かつ倒しっぱなしでも進む」体感になっているか
5. STARTボタンでのRestartが直感的か（レース中に押した場合、ゴール後/タイムアップ後に押した場合の両方）
6. BACK/VIEWボタン長押しでのReset（2026-09-04実装、今回は変更なし）が実際のXboxコントローラーでも問題なく動作するか——これまでキーボードのBackspaceでしか実機確認されていなかった
7. ゲーム実行中のコントローラー抜き差し（特に、レース中に抜いた場合キーボード操作に即座に切り替わるか、再度挿した場合に再度使えるようになるか）

## 2026-09-05（12回目）: Xboxコントローラー実機確認OK、タイヤ音量微調整＋D-pad/スティック上下でアクセル・ブレーキ

Xboxコントローラー対応（11回目）を報告したところ、ユーザーから「いいですね」と好意的な確認。続けて2点の追加要望：(1) タイヤの軋み音をもう少し小さく、(2) A/Bボタンに加えて、十字キーとアナログスティックの上/下でもアクセル/ブレーキを操作できるようにしてほしい。

### 対応：タイヤ軋み音の音量調整

`CLASSIC SQUEAL`の`volume`のみ`0.8→0.6`に変更（波形・ドライブは無変更）。数値シミュレーションで実効RMSが約0.32→約0.24に低下し、直前に確定したエンジン音（ARCADE ENGINE、実効RMS約0.24〜0.25）と近い水準になった。前回のエンジン音の1段階目の引き下げ（0.8→0.6）と同じ、控えめな下げ幅を踏襲。

### 対応：D-pad/スティック上下でのアクセル・ブレーキ

`gamepad.py`の`read_accel()`/`read_brake()`を拡張し、それぞれ以下のいずれかでTrueを返すように変更（既存のA/Bボタンは維持、追加の代替手段として実装）：

- `read_accel()`：Aボタン **または** D-pad上 **または** 左スティックを上（SDLのY軸は画面座標系と同じ慣習で「上が負の値」）へ、操舵と同じデッドゾーン（`STEER_DEADZONE=0.18`）を超えて倒した場合
- `read_brake()`：Bボタン **または** D-pad下 **または** 左スティックを下へ倒した場合

SELECT MUSIC画面のD-pad/スティック上下は今回対象外（そちらは既に左右のみでの曲送りに割り当て済みで、上下はレース中のアクセル/ブレーキ専用の追加手段のため、確定操作は引き続きAボタンとSTARTボタンのみ）。

### 検証結果

- `pytest`：既存207件→**214件**、全て成功（`tests/test_gamepad.py`にD-pad上下・スティック上下でのアクセル/ブレーキ・デッドゾーン確認を追加、`tests/test_game.py`に`Game.update()`経由での統合テストを追加）
- `python main.py --race --test-frames 60`：ヘッドレスでクラッシュなく正常終了
- `config.json`・BGM・エンジン音・レース進行・キャリブレーションは本対応で変更なし

### 実機で確認してほしいこと

1. タイヤ軋み音の新しい音量（`volume=0.6`）がちょうど良いか
2. D-pad上下・スティック上下でのアクセル/ブレーキが実際に機能するか、Aボタン/Bボタンとの併用に違和感がないか
3. （引き続き）11回目で報告した項目——`sdl_game_controller=True`の表示、抜き差し、BACK/VIEW長押しReset等

## 2026-09-05（13回目）: プレイヤー車のグラフィックを5方向ピクセルアートへ更新

Xboxコントローラー対応（12回目まで）に「いいですね」との確認を受け、ユーザーから「Xboxコントローラー対応が完了したら、次にプレイヤー車のグラフィックを更新してください」との指示。現在のプレースホルダー（矩形のみ）を、赤黒のレトロゲーム風ウェッジシェイプ・スポーツカーへ置き換える依頼で、以下が明示された：

- 後方視点、5方向（hard_left / left / straight / right / hard_right）
- 1980〜90年代アーケードレースゲーム風、実在車種・メーカーロゴの流用禁止
- Virtual Boy風の赤黒ピクセルアート、黒/透明背景、アンチエイリアスなし
- 5枚とも同一キャンバスサイズ・同一接地点
- Xboxコントローラー左スティック／D-pad／キーボードのステアリングを共通の`steering`値（-1.0〜+1.0）へ正規化し、指定の5段階閾値で切替
- 切替時のちらつき防止（補間・ヒステリシス）、デバッグ表示に`steering`値と選択中スプライト名
- 接地点・中心軸の一致、当たり判定・world_xへの影響なし、坂道の上下動追従、左右眼で同一スプライト
- 敵車グラフィック・衝突アニメーション・車種/カラー選択・本格ドリフト・背景作り込みは対象外

### 現状調査（実装前にユーザーへ報告した内容）

- **キャリブレーション済みビューポート**：`config.json`より280×282px（実機測定値、変更禁止）
- **既存のプレイヤー車描画**：`draw_car()`（矩形2つ、スケール1.0で幅44px・高さ約25px）を`Game._draw_player_car()`から呼び出し。位置は`cx = w/2 + player.x*10 + lean + ox`（水平中心、カーブ中のリーン込み）、`cy = h - 34 + player_bob`（接地基準、坂の上下動込み）——この`cx`/`cy`の計算自体は既存のまま変更せず、描画方法だけをスプライトのblitに差し替える方針とした
- **ステアリング値の流れ**：`Game.update()`内で毎フレーム`steer`（キーボードの±1デジタル値＋`gamepad.read_steer()`の連続値、クランプ済み-1〜+1）を計算し、既存の物理演算（`player.x += steer * STEER_RATE * speed_frac * dt`）に使用済み。この既存の`steer`値をそのまま「見た目用」にも再利用できると判断し、物理演算とは完全に分離した別のスムージング値（`player_visual_steer`）を新設する方針とした
- **左右眼の扱い**：`renderer.draw_world(PLAYER_CAR_DEPTH, self._draw_player_car)`が同じ`_draw_player_car`関数を左右目それぞれに1回ずつ呼ぶだけで、スプライト選択自体は`update()`内で1回だけ行われるため、左右で異なるスプライトが選ばれることは構造的に起こり得ないことを確認

### 対応：画像仕様の決定

- キャンバス：**64×48px**（280px幅のビューポートに対して約23%——旧プレースホルダーの44px/16%よりやや大きく、ピクセルアートの解像度を確保）
- 接地点/アンカー：**(32, 48)**（キャンバス下端中央）——全5枚共通
- 透明背景（`pygame.SRCALPHA`）
- パレット：赤黒4段階（BLACK＝アウトライン/窓/タイヤ、DARK＝影/バンパー、MID＝ボディ基調、BRIGHT＝ハイライト/ルーフ、GLOW＝テールランプ専用の最も明るい段階）——プロジェクト全体で確立されている「赤黒のみ」の制約を厳守し、テールランプも実際のオレンジ/黄色ではなくパレット最明部の赤系で表現

### 対応：ピクセルアートの生成方法

画像生成ツールが無いため、`assets/cars/player/generate_placeholder_sprites.py`を新規作成し、pygameの描画命令（`Surface.fill()`による矩形塗り、16×12の「ネイティブピクセル」グリッドを1マス4×4pxとして直接その解像度で描画）で5枚を生成した。拡大処理は一切行わない設計（実行時も常に1:1でblitするため、「拡大時はnearest-neighbor」の要件はスケーリング自体を発生させないことで満たしている）。

- `straight`：左右対称にハンドデザイン（ルーフ、リアウィンドウ、ボディ、バンパー+テールランプ、タイヤの5要素）
- `right`・`hard_right`：`straight`の各行の水平方向スパンを、行（上から下）に応じて増加する量だけシフトする「シアー（せん断）」変形で近似（ルーフに近い上部ほどシフト量が小さく、テール側の下部ほど大きい——「ルーフを支点に尾部が振れる」という簡易的な3/4視点の近似）
- `left`・`hard_left`：`right`・`hard_right`を`pygame.transform.flip()`で水平反転して生成（指示通り、片側を基準に反転）

生成後、実際にヘッドレスレンダリングでキャリブレーション済みビューポート内に合成した画像を保存し、Readツールで視覚確認しながらパレット（テールランプの視認性、タイヤと影の区別）を2回調整した。

### 対応：`game.py`への統合

- `PLAYER_SPRITE_KEYS`・`PLAYER_SPRITE_CANVAS_SIZE`・`PLAYER_SPRITE_ANCHOR`・`PLAYER_SPRITE_THRESHOLDS`（-0.55/-0.15/+0.15/+0.55）・`PLAYER_SPRITE_HYSTERESIS`（0.05）・`PLAYER_VISUAL_STEER_SMOOTHING`（6.0/秒）を新規定数として追加
- `_load_player_sprites()`：5枚のPNGをプロセス起動後1回だけ読み込みキャッシュ（Restart/Resetのたびに`Game`が再生成されても毎回ディスクから再読込しない）。**いずれか1枚でも読み込みに失敗、またはキャンバスサイズが不一致な場合は5枚すべて不採用**とし、一部だけプレースホルダー矩形に戻るような中途半端な状態を避ける設計
- `Game.__init__`/`_restart()`：`player_visual_steer=0.0`、`_player_sprite_index`をstraightにリセット
- `Game.update()`：既存の`steer`計算直後に、`player_visual_steer`を`steer`へ向けて時間ベースで補間（`_restart()`同様、レース中以外は更新されない——既存の`bg_offset`等と同じパターンを踏襲）、続けて`_update_player_sprite_index()`を呼びスプライトのインデックスを更新
- `_update_player_sprite_index()`：ヒステリシス付きの境界判定。現在のインデックスの前後の境界にそれぞれ`PLAYER_SPRITE_HYSTERESIS`のマージンを加え、それを超えない限り切り替わらない設計。1フレームで複数カテゴリをまたぐ入力にも正しく追従するようループ処理（実装中、値を直接大きくジャンプさせるテストで「1フレーム1段階しか進まない」設計の不備を発見し、複数段階に対応するよう修正した）
- `_draw_player_car()`：`draw_car()`の呼び出しをスプライトのblit（`surf.blit(sprite, (cx - anchor_x, cy - anchor_y))`）に置き換え。`cx`/`cy`の計算式自体は一切変更していないため、接地点・中心軸は構造的に不変
- `_draw_debug_overlay()`：`player sprite: steer=+0.xx -> <sprite名> (assets loaded/MISSING...)`の行を追加

### 検証結果

- ヘッドレスレンダリングでキャリブレーション済みビューポート内に5パターン全てを合成し、Readツールで視覚確認（道路・HUDとの重なり、接地位置のブレなし、左右反転の対称性を確認）
- `pytest`：既存214件→**236件**、全て成功。新規テストで以下を確認：
  - 直進・左保持（keyboard/D-pad）・右保持（keyboard/D-pad）でそれぞれ`straight`/`hard_left`/`hard_right`へ収束
  - アナログスティックの中間デフレクションで`left`/`right`（keyboard/D-padには到達できないカテゴリ）に収束
  - デッドゾーン内の入力で`straight`のまま
  - 境界付近の値を交互に与えてもスプライトが切り替わらない（ヒステリシス確認）
  - 大きな1フレームジャンプでも正しいカテゴリへ到達（前述の実装不備の回帰テスト）
  - スプライトの有無で`player.x`/`player.z`/`speed`/`collision_cooldown`の軌跡が完全一致（当たり判定・world_xへの無影響を確認）
  - 5枚全てのキャンバスサイズが一致
  - 画像ファイル欠損時にクラッシュせずプレースホルダー矩形へフォールバック
  - `Game`インスタンスを複数生成してもスプライトの読み込みは1回のみ（キャッシュ共有）
  - `_draw()`（左右両目分の描画）を呼んでも`_player_sprite_index`が変化しない（左右で同じスプライトが使われることの裏付け）
  - Restart/Resetで`straight`・`player_visual_steer=0.0`へ戻る
  - 坂区間で`_draw()`がクラッシュせず、`player_bob`が実際に非ゼロになる（上下動の維持を確認）
  - デバッグオーバーレイ表示時にクラッシュしない
- `python main.py --race --test-frames 180`：クラッシュなく正常終了
- `config.json`・BGM・エンジン音・タイヤ軋み音・敵車・入力・レース進行・キャリブレーションは本対応で変更なし

### 実機で確認してほしいこと

1. 左へステアリングした際に実際に`hard_left`/`left`の画像が表示され、視覚的に「左を向いている」と感じられるか（シアー変形による簡易的な3/4視点表現が、実機のレンズ越しでも方向として認識できるか）
2. 右も同様（左を反転しただけなので、左が正しければ右も対称的に正しいはずだが、レンズの左右非対称性等の影響がないか一応確認）
3. キーボード・Xboxコントローラーの十字キー・アナログスティックいずれの入力でも、意図通りに切り替わるか
4. 境界付近（例えば緩やかに左右へ小刻みに入力した場合）で画像がちらつかないか
5. 坂道での上下動、カーブでのリーン効果が従来通り機能しているか（スプライト化後も見た目が破綻していないか）
6. サイズ感（64×48px、旧プレースホルダーよりやや大きい）が実機で見て違和感がないか——気になる場合は`PLAYER_SPRITE_CANVAS_SIZE`ではなく生成スクリプトの`PIX`定数を調整することで再生成可能

## 2026-09-05（14回目）: プレイヤー車を本番用スプライトシート（外部作成PNG）へ差し替え

前回（13回目）で仮素材として扱うと決めた16×12グリッド由来のプレースホルダー5枚を、ユーザーが外部で作成した本番用スプライトシート画像へ差し替えた。指示は「既存の5方向切り替え処理は維持し、現在の仮スプライトだけをこの画像へ差し替える」というもので、`_update_player_sprite_index()`等のロジックには一切手を加えていない。

### 対応：2種類のスプライトシートが提示された

1. 1枚目（1774×887px、左からhard_left/left/straight/right/hard_right の順で赤黒のウェッジ型スポーツカー5台）を確認したところ、指示通りアルファチャンネルを見てみると**全面255（完全不透明）で、実際には透過情報が存在しなかった**。背景に見えていた市松模様は実際にはRGB側にほぼ無彩色のグレー（R,G,B≈13〜21）として焼き込まれていた。そのためアルファではなく色相ベースのヒューリスティック（`R - max(G,B) > 15`、赤成分が緑・青を一定以上上回れば車体とみなす）に切り替えて対応し、ヒストグラムで背景と車体が明確に2峰性に分離することを確認した上で5領域の検出に成功した（境界はすべて`bottom_y=540`で一致）。
2. 直後にユーザーから「こちらの画像を使ったほうが処理がスムーズであれば、こちらを使ってください」と2枚目（1983×793px）が提示された。確認すると**こちらは実際にアルファチャンネルを持っており**（四隅の値が0、画像全体で0〜255のフルレンジ）、1枚目のような色ヒューリスティックが不要だったため、こちらを採用した。単純な`alpha > 10`のしきい値だけで5領域がきれいに分離でき（境界の下端はいずれも497〜498pxで実質一致）、抽出処理・保守性の両面でこちらのほうが優れていると判断した。
3. 元のスプライトシート（採用した2枚目）は`assets/cars/player/source_spritesheet.png`として保存した（指示通り）。

### 対応：抽出・合成スクリプト

新規に`assets/cars/player/build_from_spritesheet.py`を作成（`generate_placeholder_sprites.py`は指示通り変更せず、フォールバック/開発用ツールとして温存）。処理内容：

- `source_spritesheet.png`のアルファチャンネルから5台分の連続領域（列単位でアルファ>10の有無を走査）を検出し、各領域内でタイトなバウンディングボックスを求める
- 全5台の最大幅・最大高さ＋パディング6pxで共通の「ネイティブ解像度キャンバス」を用意し、各車のバウンディングボックス下端（タイヤ接地位置）をキャンバス内の同一Y座標へ、バウンディングボックス水平中心をキャンバス内の同一X座標へ揃えて配置（ソース側で5台とも接地Yが1px以内で一致していたため、縦方向のオフセット推定は不要だった）
- `pygame.transform.scale()`（`smoothscale()`ではない、アンチエイリアスなしのnearest-neighbor相当）で最終インゲームサイズへ一括縮小
- `player_hard_left.png`等、既存と同じファイル名で上書き保存

### 対応：最終サイズと`game.py`の定数更新

- 最終キャンバスサイズを**88×48px**に決定（280px幅の左目ビューポートに対し約31.4%。旧プレースホルダー64×48pxの23%よりやや大きめ——今回の本番アートは情報量が多く、旧プレースホルダーと同じ64px幅では細部が潰れると判断したため）
- `game.py`の`PLAYER_SPRITE_CANVAS_SIZE`を`(64, 48)`→`(88, 48)`、`PLAYER_SPRITE_ANCHOR`を`(32, 48)`→`(44, 48)`へ更新（キャンバス下端中央、5方向切り替えロジック自体は無変更）。`_load_player_sprites()`はサイズ不一致で全5枚を不採用にする設計のため、この定数更新を怠ると新スプライトが読み込まれずプレースホルダー矩形へフォールバックしてしまう点に注意

### 検証結果

- ヘッドレスレンダリングで5方向すべてを6倍拡大した確認画像（緑線＝接地アンカー行）を生成し、Readツールで視覚確認——全5枚とも接地行・水平中心が一致していることを確認
- キャリブレーション済み左目ビューポート（280×282px）内へ実際に描画した5方向の画面キャプチャも生成し、道路・HUDとの重なり、サイズ感を確認——ユーザーへ両画像を送付済み
- `pytest`：**236件、全て成功**（`PLAYER_SPRITE_CANVAS_SIZE`を動的参照しているテストのみのため、定数変更後も無修正で通過）
- `python main.py --race --test-frames 120`：クラッシュなく正常終了
- `config.json`・BGM・エンジン音・タイヤ軋み音・敵車・入力・レース進行・キャリブレーション・5方向切り替えロジック自体は本対応で変更なし

### ユーザーへ報告した実際のインゲーム表示サイズ

- スプライトキャンバス：88×48px
- キャリブレーション済み左目ビューポート（280×282px）に対する比率：横31.4%、縦17.0%
- スプライトは常に1:1ピクセルでblitされ、`StereoRenderer`は視差（左右オフセット）以外のスケーリングを一切行わないため、上記が実機での実寸に一致する

## 2026-09-05（15回目）: 敵車（一般車）を6種の本番スプライトへ差し替え

`CLAUDE_CODE_ENEMY_CARS_INSTRUCTIONS.txt`と6種の敵車スプライトシート（`sports_coupe`/`boxy_sedan`/`compact_hatchback`/`panel_van`/`muscle_car`/`pickup_truck`、シート内配置は左上から2行×3列）が提供され、これまで`draw_car()`の矩形で描画していた敵車を本番アートへ差し替えた。指示書の作業手順に従い、まず既存の敵車描画・当たり判定・立体視処理の所在を調査し、変更予定ファイルと実装方針を報告してユーザーの承認を得てから実装した。

### 対応：背景除去（フラッドフィル）

シートのアルファは全面255（透過なし）、四隅はほぼ白（RGB≈253,253,253）で、プレイヤー車1枚目シートと同様に市松模様がRGB側に焼き込まれていた。指示書が明示的に警告する通り、単純な明度しきい値では車内の窓・テールランプ・ハイライト・ナンバープレートまで消えてしまうため、**シート外周から連結した「明るい（最大チャンネル>200）かつ低彩度（最大-最小チャンネル<30）」画素だけをフラッドフィルで検出**し、それだけをalpha=0にする方式を採用した（新規`scripts/build_enemy_sprites.py`）。フラッドフィル自体は1画素ずつのBFSではなく、候補マスクに制限した反復ダイレーション（差分が無くなるまで4方向に1画素ずつ拡張）で実装し、1536×1024pxのシート全体でも1秒未満で完了することを確認した。窓・ハイライト等、車体内部で外周と非連結な明るい領域はそのまま残ることを、生成結果の目視確認（後述）で確認した。

### 対応：切り出し・共通キャンバスへの配置

指示書の「バンやピックアップの車高差・車幅差は残す」という要件から、プレイヤー車のときとは異なり**全6台を同一の縮小率**（最も大きい車がターゲットボックス92×56pxへ収まるよう1つだけ計算）でスケーリングし、それぞれの見た目の大きさの違いをそのまま保持した。共通の透明キャンバス（結果的に68×56px）へ、各車のバウンディングボックス下端中心を同一アンカー座標（`(34, 56)`）に揃えて配置。実際の生成結果を検証したところ、6台とも接地行が51px（56px中）で完全一致、水平中心も34.0〜34.5pxの範囲に収まることを確認した。

### 対応：ゲームへの統合（`game.py`）

- `TrafficCar`に`sprite_id: str | None`フィールドを追加
- **重要な安全対策**：ユーザーから「車種抽選で既存のrngを消費すると、その後の車線・速度・配置の乱数列が変わる可能性がある」との指摘を受け、`_build_traffic()`内の既存レーン抽選（`random.Random(42)`）とは完全に独立した**専用の`random.Random(ENEMY_SPRITE_RNG_SEED)`（seed=20260905）を、既存のレーン/位置/速度ループが全て完了した後に別ループとして追加**する設計にした。既存の`rng`へ車種抽選を割り込ませない（既存の`rng.choice()`呼び出し列を一切変更しない）ことを、変更前後で交通配置（z・x・speed）が完全一致することを確認するテスト（後述）で担保した
- `_load_enemy_sprites()`：`_load_player_sprites()`と同じ「1枚でも欠損/サイズ不一致なら6枚とも不採用」パターンでキャッシュ読み込み
- `_draw_traffic_car()`：既存の`sw`由来の`scale = max(0.35, sw / (ROAD_WIDTH * 3.5))`（変更なし）をそのままスプライトの表示サイズ・アンカーオフセットへ適用。`pygame.transform.scale()`（**`smoothscale()`ではない**、nearest-neighbor、アンチエイリアスなし）で毎フレーム縮小し、左右両目のクロージャに入る前に1回だけ計算することで、左右眼が完全に同一のスケール済みSurfaceを共有する構造にした。アセット欠損時は既存の`draw_car()`矩形へフォールバック
- 衝突判定（`player.z`/`car.z`/`player.x`/`car.x`のみ参照）、丘の遮蔽（`_sprite_visible`、`sy`のみ参照）、立体視（`renderer.draw_world`が視差オフセットのみ付与）はいずれも無変更

### 出力・保存先

- `assets/cars/enemies/{sports_coupe,boxy_sedan,compact_hatchback,panel_van,muscle_car,pickup_truck}.png`（新規6枚）
- `assets/source/enemy_cars_sheet.png`（元シート保存）
- `scripts/build_enemy_sprites.py`（背景除去・切り出し・整列スクリプト、新規）

### 検証結果

- 交通配置の回帰テスト：変更前（`rng.choice()`のみでsprite_id無し）に実際に取得した22台分の`(z, x, speed)`をテストへハードコードし、変更後の`_build_traffic()`の出力と完全一致することを確認（`test_traffic_lane_position_and_speed_are_unchanged_by_sprite_id_assignment`）
- rng独立性の直接検証：仮に車種抽選が既存`rng`を共有していた場合に得られる列を別途計算し、実際の結果と**一致しない**こと、および独立した`ENEMY_SPRITE_RNG_SEED`から計算した期待列とは**一致する**ことの両方を確認するテストを追加（`test_enemy_sprite_rng_is_independent_of_the_lane_rng`）
- `sprite_id`が生成時に1回だけ決まり、30フレーム分の`update()`後も不変であることを確認
- Restart時も同じ2つの固定シードにより、車種の並びが毎回同一であることを確認
- 6枚全てがRGBAで`alpha`の最小値0・最大値255を持つ（実際に透過があること）ことを確認
- 6枚が共通キャンバスサイズを共有し、接地行の差が3px以内（車種間で接地アンカーが揃っている）ことを確認
- 距離が近づくほど表示幅が単調に増加することを、`project()`を直接使ったテストで確認
- `pygame.transform.scale`が実際に呼ばれ、`pygame.transform.smoothscale`は一度も呼ばれないことを、両関数をモンキーパッチして呼び出し回数を数えるテストで確認
- 画像欠損時のフォールバック（`draw_car()`）がクラッシュしないことを確認
- 複数`Game`インスタンス生成時にスプライトの読み込みが1回のみ（キャッシュ共有）であることを確認
- 描画（左右両目分）を呼んでも`sprite_id`が変化しないことを確認
- `pytest`：236件→**248件**に増加、全て成功
- `python main.py --race --test-frames 180`：クラッシュなく正常終了
- `config.json`：本対応前後で差分なしを確認
- 実際のキャリブレーション済みビューポート（280×282px）へ6車種それぞれをレンダリングした確認画像と、6車種を並べた確認画像（緑線＝接地アンカー行）をユーザーへ送付済み

### 実機で確認してほしいこと

1. 遠方でも敵車の種類が見分けられるか
2. 接近時に粗すぎる、またはプレイヤー車よりリッチすぎる印象がないか
3. 坂道で路面から浮いたり沈んだりしないか
4. 左右眼で形・大きさ・高さが一致しているか
5. 追い越し時の立体感と視差が自然か
6. 車種の出現比率（sports_coupe/boxy_sedan/compact_hatchback各20%、panel_van/muscle_car各15%、pickup_truck 10%）と画面の賑やかさが適切か

## 2026-09-05（16回目）: 実機確認で判明した「接近時のpanel_van巨大化」を修正、遠方視認性とのバランスを再設計

15回目対応後の実機確認で、「近づいたパネルバンの表示が大きすぎる」とのフィードバック。ユーザーからの追加要望は、(1) 敵車がプレイヤー車と同じ奥行きに来たと仮定した場合、車種を問わずプレイヤー車とおおむね同じ大きさ（±5%以内）に見えること、(2) 6車種すべてに同一の縮小率を適用する方式をやめ、各車を個別に正規化すること、(3) バン・ピックアップの形状差は残しつつ、極端な巨大化を防ぐこと、(4) 推奨する見た目の目安（sports_coupe＝低め標準幅、boxy_sedan＝標準、compact_hatchback＝標準より5%小さい、panel_van＝標準幅で高さのみ10〜15%増、muscle_car＝標準より5%幅広、pickup_truck＝標準幅でバンより低め）。

### 原因

`scripts/build_enemy_sprites.py`（15回目実装）は6台に**同一の縮小率**を適用し、共通キャンバスへ配置していた。共通キャンバス自体は6台で同一サイズだったが、各車がそのキャンバスに占める不透明領域（実際に見える車体）の比率は車種ごとに大きく異なっていた（例：panel_vanは高さ方向の充填率が約84%、sports_coupeは約54%）。`_draw_traffic_car()`は共通キャンバスの**全体**（透明な余白込み）を距離スケールで一律に拡大していたため、キャンバス自体は全車種で同じ大きさに描画されていても、その中の「実際に見える車体」の大きさは車種ごとの充填率にほぼ比例して異なり、panel_vanのように充填率が高い車種ほど視覚的に大きく感じられていた。

さらに調査の過程で、既存の距離スケール式（`sw / (ROAD_WIDTH * 3.5)`、旧`draw_car()`矩形も含めて元々使われていたもの）は、敵車がプレイヤー車と同じ深さ（`world_z == cam_z`、`project()`の`trans_z`が`SEGMENT_LENGTH`で下限に達する状態）に達すると、理論上ビューポート幅（280px）の2倍以上にまで拡大する設計だったことが判明した（実際にはこの深さでは画面下端を超えて表示されないため、それ自体は今回まで問題として顕在化していなかった）。

### 対応：`build_enemy_sprites.py`を個別正規化方式へ変更

6台共通の縮小率をやめ、各車を**個別の目標(幅, 高さ)ピクセルサイズへ独立に正規化**する方式へ変更（`WIDTH_MULT`/`HEIGHT_MULT`辞書、`boxy_sedan`＝標準=1.00を基準）。

- 幅：sports_coupe 1.00 / boxy_sedan 1.00 / compact_hatchback 0.95 / panel_van 1.00 / muscle_car 1.05 / pickup_truck 1.00（全車種、標準の±5%以内に収める）
- 高さ：sports_coupe 0.78（低め） / boxy_sedan 1.00 / compact_hatchback 0.95 / panel_van 1.125（+12.5%、指示の10〜15%の中間） / muscle_car 1.00（幅のみ変更、高さは標準のまま） / pickup_truck 0.95（標準よりやや低く、パネルバンより明確に低い）
- 各車のクロップを`pygame.transform.scale()`（nearest-neighbor、アンチエイリアスなし）でこの目標サイズへ**非等方（縦横別倍率）**に変形——ソース写真自身のアスペクト比を尊重するより、ユーザー指定の縦横比を正確に再現することを優先。共通キャンバス（結果的に75×58px）・下端中央アンカーは維持
- 実測結果：6台とも接地行が53px（56px中）で一致、幅は60〜66px（標準63pxの±5%以内、muscle_carのみ+4.8%相当）

### 対応：`game.py`の敵車スケーリングを全面再設計

1回目の修正案（`ENEMY_SPRITE_DISPLAY_SCALE`という単一定数を距離スケールに掛けるだけの方式）は、「プレイヤー車と同じ深さ」での一致を正確に実現できたが、副作用として60〜90m先の敵車が数px程度まで縮小し、「遠方でも車種が見分けられるように」という当初の指示書要件を損なうことが判明したため、ユーザーに一度中間報告し、追加の調整方針を確認した。

ユーザーからの指示（物理的に厳密な遠近法ではなくゲームとしての視認性を優先、ただし距離に応じた非線形補正とし、smoothstepで滑らかに、近接時は1.0へ戻す）を受け、最終的に以下の設計へ変更：

- 敵車の表示幅は、もはや生の距離スケール（`sw / (ROAD_WIDTH * 3.5)`）に比例させず、**`ENEMY_SPRITE_TARGET_RATIO_POINTS`という「距離tz→目標比率（プレイヤー車幅に対する割合）」の制御点列**を直接補間して決定する方式へ変更（`_enemy_target_ratio()`）。制御点：`(3.0, 1.00)`（同じ深さ＝100%）・`(30.0, 0.40)`・`(60.0, 0.175)`・`(90.0, 0.10)`。90m以遠は0.10で一定（距離スケール自体が`0.35`下限でプラトーになるのと同じ考え方）
- 制御点間は`_smoothstep()`による滑らかな補間。**「boost倍率」を距離スケールに掛け算する初期案から、「目標比率」そのものを直接補間する方式へ設計変更**した理由は、tz全域で密にサンプリングしたテスト（`test_enemy_car_display_width_never_dips_as_it_gets_closer`）で、boost方式だとtz≈10〜20の範囲でわずかな非単調（一瞬サイズが増えてから減る「ポップ」）が生じることを検出したため。目標比率の制御点はtzに対して単調減少（3.0→100%、30→40%、60→17.5%、90→10%）に選んであるため、同じ区間を滑らかに補間しても数学的に単調性が保証される
- `draw_car()`のプレースホルダー矩形フォールバックは、この目標比率方式とは無関係に、従来通りの生の距離スケールをそのまま使用（変更なし）
- 安全策として`ENEMY_SPRITE_MAX_WIDTH_RATIO=1.05`のハードクランプを追加——目標比率カーブの計算結果によらず、どの車種・どの距離でも敵車の表示幅がプレイヤー車幅の105%を超えないことを保証（muscle_carの`WIDTH_MULT=1.05`が同じ深さの計算点でちょうど105%相当になるため、この安全策が実質的にその境界を保証する形になる）
- クランプ・目標比率とも「その車自身の不透明ピクセル幅」基準で計算（共通キャンバスの全体幅ではなく）。各スプライトの不透明幅は`Game.__init__`で1回だけ計算しキャッシュ（`_opaque_sprite_width()`、`self._enemy_sprite_opaque_widths`）、毎フレームのアルファ読み取りを避けている

### 検証結果

- `pytest`：248件→**254件**に増加、全て成功。新規テストで以下を確認：
  - 90m/60m/30m地点でboxy_sedanの表示幅がユーザー指定の目標レンジ（8〜12%/15〜20%/35〜45%）に収まる
  - 全6車種・tz=3.0〜302.5を0.5刻みで密にサンプリングし、距離が縮まるにつれ表示幅が一度も減少しない（真の単調性、ポップ無し）ことを直接検証
  - 全6車種・複数距離で表示幅がプレイヤー車幅の105%を一度も超えない
  - 「プレイヤー車と同じ深さ」でのプレイヤー車幅との一致（±5%以内）、6車種間の相対的な幅の一致（±5%以内）は前回同様維持
  - 既存の交通配置回帰テスト・rng独立性テストは無変更のまま成功（本対応は描画・アセット生成のみが対象）
- `python main.py --race --test-frames 180`：クラッシュなく正常終了
- `config.json`：本対応前後で差分なしを確認
- ヘッドレスレンダリングで、丘・遮蔽の影響を排除した平坦区間に固定配置した敵車を撮影し、(1) 修正前後の同一距離（tz=10、衝突圏のすぐ外側）での比較画像、(2) 全6車種×6距離（90/60/45/30/15m/最接近）の比較グリッド画像をユーザーへ送付し、確認を得た

### 実機で確認してほしいこと

1. 接近時、車種を問わずプレイヤー車と同程度以下のサイズに見えるか（特にpanel_van）
2. 60〜90m先の敵車が、以前より見分けやすくなっているか（今回追加した遠方ブースト）
3. 距離が変化する際に敵車のサイズが不自然にポップ/ジャンプして見えないか
4. muscle_carが他よりわずかに幅広く見えるか、panel_vanが標準幅のまま高さだけやや高く見えるか（車種ごとの形状差が意図通り読み取れるか）
5. 坂道・追い越し時の見え方（15回目の確認項目と同様）

## 2026-09-05（17回目）: 敵車の接近時サイズを実機A/B比較で80%相当に確定

16回目対応後の実機確認で、「近づいたパネルバンの表示は改善したが、依然として敵車がプレイヤー車より体感で約1.2〜1.5倍大きく見える」とのフィードバック。加えて、比較の基準を「現在のキャンバス幅や論理基準値ではなく、実際に描画されたプレイヤー車と敵車の不透明画素bbox（visible bounds）を基準に比較すること」との指摘があった。

### 対応：測定方法をハードコード定数から実測値へ変更

`PLAYER_REFERENCE_WIDTH_PX=71`・`ENEMY_REFERENCE_OPAQUE_WIDTH_PX=63`という、16回目に一度だけ測定してハードコードした定数を比較基準に使っていたことが、実機での見え方とのズレの一因になりうると判断。`Game.__init__`で、実際にロードされたプレイヤー・敵車スプライト（`self._player_sprites["straight"]`・`self._enemy_sprites["boxy_sedan"]`）から**毎回実測**する方式に変更（`self._player_reference_width_px`／`self._enemy_reference_opaque_width_px`）。モジュール定数は「アセット読み込み失敗時のみ使うフォールバック値」という位置付けに変更した。

### 対応：最近接時の目標比率を100%→85%へ

60m（約17.5%）・90m（約10%）・30m（約40%）は実機で視認性良好と確認済みのため維持。**最近接時（同深度）の目標比率のみ100%→85%に変更**（`ENEMY_SPRITE_TARGET_RATIO_POINTS`の該当制御点、`ENEMY_SPRITE_MAX_WIDTH_RATIO`のクランプも105%→85%）。30m〜同深度間は既存通りsmoothstepで滑らかに補間。「プレイヤー車と同じ奥行き」（`world_z == cam_z`、`project()`のtrans_zがSEGMENT_LENGTHで下限）は、投影計算上ビューポート下端より下に投影され実際には表示されないことも判明したため、その旨をあわせて報告した。

`pytest`は254件、`python main.py --race --test-frames 180`・`config.json`無変更も確認。30m/15m/最接近（画面下端に隠れる手前、約5m）の3距離×全6車種で、黄＝キャンバス外形・シアン＝プレイヤー車不透明bbox・緑＝敵車不透明bboxを重ねた比較画像を生成しユーザーへ送付した。

### 対応：実機A/Bツール（`F7`、一時的）で最終値を確定

上記の85%調整後も実機では単一の数値報告だけでは判断が難しいとのことで、「未使用のキーボードキー（例：F7）で100%→90%→80%→100%を循環切替できる一時的なデバッグ機能」を要望された。`ENEMY_SIZE_DEBUG_MULTIPLIERS=(1.0, 0.9, 0.8)`を追加し、`F7`で循環切替、切替直後2秒間「ENEMY SIZE: N%」を画面表示、`D`キーのデバッグオーバーレイにも現在値を常時表示するようにした。倍率は「距離カーブ＋クランプで最終的に求めた敵車表示サイズ」へ、全6車種に同一の値を掛ける方式（プレイヤー車・衝突判定・Z座標・交通配置・立体視・坂道追従・`config.json`は無変更）。`pytest`は254件→**262件**に増加、全て成功（循環動作・フラッシュ表示・6車種への均等適用・数値一致・各倍率での単調性・プレイヤー等への無影響を検証）。この時点ではユーザーからの比較結果が出るまでコミット・pushを保留した。

実機で3案を聞き比べた結果、**「80%がいい感じです」**との回答。

### 対応：80%を恒久反映、デバッグツールを削除

80%案（＝85%目標値の0.8倍＝同深度で約68%）を新しい基準として`ENEMY_SPRITE_TARGET_RATIO_POINTS`・`ENEMY_SPRITE_MAX_WIDTH_RATIO`へ恒久的に反映した。F7の実機A/Bツールは「距離カーブ全体（30〜90mも含む）に一律で倍率を掛ける」という一時比較用の簡略な実装だったため、実機で確認・承認されたのはこの「全区間0.8倍」という体験そのものと判断し、90m・60m・30mの目標比率もそれぞれ0.8倍した値（10%→8%、17.5%→14%、40%→32%）を新しい正式カーブとして採用した。

- `ENEMY_SPRITE_TARGET_RATIO_POINTS = ((3.0, 0.68), (30.0, 0.32), (60.0, 0.14), (90.0, 0.08))`
- `ENEMY_SPRITE_MAX_WIDTH_RATIO = 0.68`
- `F7`デバッグツール一式（`ENEMY_SIZE_DEBUG_MULTIPLIERS`・`_cycle_enemy_size_debug`・キーバインド・フラッシュ表示・デバッグオーバーレイ行）は、指示通り比較確定後に削除（「一時的なデバッグ機能」との位置付け通り）

### 検証結果

- `pytest`：262件→（F7関連8件を削除）**254件**、全て成功。最近接時の目標比率が約68%に収まること、90/60/30mの目標比率が新しい値（8%/14%/32%）に収まること、クランプ（0.68）が全車種・全距離で守られること、全距離で単調増加しポップしないことを直接検証
- `python main.py --race --test-frames 180`：クラッシュなく正常終了
- `config.json`：本対応前後で差分なしを確認
- プレイヤー車・衝突判定・交通配置（車線/位置/速度/sprite_id）・立体視・坂道追従はいずれも無変更

### 実機で確認してほしいこと

1. 接近時のサイズ感が実際に「いい感じ」のまま安定しているか（F7ツール削除後、恒久反映されたコードでの最終確認）
2. 60〜90m先の敵車の見分けやすさに問題がないか（0.8倍した後でも視認性が保たれているか）
3. 坂道・追い越し時の見え方に違和感がないか

## 2026-09-05（18回目）: 道路脇にヤシの木スプライトを配置

`CLAUDE_CODE_PALM_TREES_INSTRUCTIONS.txt`の指示に従い、既存の道路脇デコレーション（`draw_tree()`による手続き型の木）はそのまま残しつつ、疾走感と南国の海岸道路らしさを強化する目的で、ユーザー提供の3列×2行スプライトシート（`palm_trees_sheet.png`）から6種類のヤシの木を切り出して配置した。

### 対応：背景除去とスプライト切り出し（`scripts/build_palm_sprites.py`）

シートの背景はシアン系のクロマキーだが完全な単色ではないため、単純なRGB一致では削除しなかった。各セル（3×2に分割）ごとに外周からの4連結フラッドフィルを行い、「シアン優勢（G・Bが高くRが十分低い）」かつ外周とつながっている画素だけをalpha=0にする方式を採用。赤・暗赤・黒・薄赤の木の画素はそのまま残る。各セルの不透明領域のbboxで切り出し、根元中央を共通アンカーとする透明キャンバス（`PALM_SPRITE_CANVAS_SIZE`、アンカーは`PALM_SPRITE_ANCHOR`）へ配置し、`assets/scenery/palms/{palm_straight,palm_lean_left,palm_lean_right,palm_short_wide,palm_windblown,palm_pair}.png`としてRGBA PNGで保存。元シートは`assets/source/palm_trees_sheet.png`に保存済み。拡大縮小は常に`pygame.transform.scale()`（nearest-neighbor）のみを使用し、`smoothscale()`は一切呼ばない。

### 対応：配置ロジック（`_build_palms()`）

既存のコース生成・敵車配置が使う`random.Random(42)`／`random.Random(ENEMY_SPRITE_RNG_SEED)`とは完全に独立した専用の`random.Random(PALM_RNG_SEED)`を、他の乱数列がすべて確定した後に1回だけ消費する形で新設。セグメント60〜1050の範囲に、28〜40セグメント間隔で配置し、左右は基本ランダムに反転させつつ同じ側が3回以上連続しないようにし（`PALM_SAME_SIDE_MAX_STREAK=2`）、12.5%の確率で`palm_pair`（2本組）を選ぶ。ヤシの木には当たり判定を追加していない（装飾のみ）。

### 対応：描画・遠近投影・立体視・坂道追従（`_draw_palm()`）

新しい投影パイプラインを作らず、既存の道路脇デコレーション（`_draw_decor_object`）と同じ`world_x = seg.world_x + side * (道路端からのオフセット)`・`elevation_at()`による坂追従・`_sprite_visible()`による頂上遮蔽をそのまま再利用した。左右眼は同じsprite_id・同じ縮尺・同じ高さで描画し、X座標のみ既存のdepth/parallax計算（`renderer.draw_world`）で変える。各ヤシの実際の見た目サイズは、キャンバスの生の高さではなく「読み込んだPNGの不透明画素の実測高さ」を`palm_straight`基準に比較した`height_ratio`で決めており、`palm_short_wide`は低く、`palm_pair`は1本のヤシとほぼ同じ高さになる。`PALM_MAX_HEIGHT_VIEWPORT_FRAC`（ビューポート高さの80%）を超える手前で`PALM_NEAR_CULL_MARGIN`分の余裕を持ってカリングし、「最大サイズで固まって見える」不自然さを避けている。

### 対応：パフォーマンス（量子化キャッシュ）

毎フレームの`pygame.transform.scale()`呼び出しを避けるため、目標高さを`PALM_CACHE_SIZE_QUANTUM_PX=2`px単位に量子化した`(sprite_id, 量子化高さ)`をキーとするLRUキャッシュ（`OrderedDict`、上限`PALM_CACHE_MAX_ENTRIES=200`）を実装。同じ距離のヤシが左右眼・複数フレームにわたって同じスケール済みSurfaceを再利用する。画像読み込み失敗時は、既存の`draw_tree()`によるプレースホルダー描画へフォールバックし、ゲームをクラッシュさせない。

### デバッグ表示

`D`キーの既存デバッグオーバーレイに、表示中のヤシの木の本数・sprite_id・Z距離・左右どちらの側かを示す1行を追加（`_palm_debug_line()`）。

### 検証中に見つかった2件のテスト不具合と修正

実装自体は正しかったが、追加した2件のテストに以下の不具合があり、`pytest`が失敗していた（ゲームロジックのバグではなく、テストの検証方法の誤り）。

1. **`test_palm_scaled_sprite_cache_is_shared_across_both_eyes_and_bounded`**：`pygame.transform.scale`をグローバルにフックしてヤシのキャッシュ効果を検証していたが、既存の敵車スプライトの拡縮（`_draw_traffic_car`側、意図的に毎フレーム非キャッシュ）が同じフレーム内でたまたま同じサイズにスケールされ、ヤシのキャッシュミスと誤認されていた。`g.traffic = []`で交通車両を除外し、ヤシのスケール呼び出しだけを計測するよう修正。
2. **`test_palm_hidden_behind_hill_crest_until_crested`**：「頂上を越えたら見える」を確認する2つ目のチェックポイントに`HILL_START + 160`を使っていたが、対象のヤシは`HILL_START + 130`地点に置かれているため、プレイヤーが既にそのヤシを通り過ぎた（`world_z < cam_z`）状態になっており、`_draw_palm`が持つ「カメラより後ろのオブジェクトは描画しない」という別のカリング条件に引っかかっていた（参照元の`test_object_behind_hill_crest_is_hidden_until_crested`は`_sprite_visible`を直接呼ぶため、この条件を経由せず問題が表面化していなかった）。チェックポイントを`HILL_START + 100`（頂上は越えているが対象のヤシより手前）に修正。

### 検証結果

- `pytest`：275件、全て成功（上記2件の修正後）
- `SDL_VIDEODRIVER=dummy SDL_AUDIODRIVER=dummy python main.py --race --test-frames 180`：クラッシュなく正常終了
- `config.json`：本対応前後で差分なしを確認
- ヘッドレスレンダリングで実際にヤシが描画された1フレームを保存して目視確認：道路脇にヤシのシルエットが赤黒の画風で認識できる大きさ・位置で表示されることを確認
- 既存の敵車22台の配置・立体視・坂道遮蔽・プレイヤー車・衝突判定・BGM/SE/コントローラー操作は無変更（ヤシ関連コードはすべて追加のみ）

### 実機で確認してほしいこと

1. ヤシの木が遠方でも形として認識できるか
2. 接近時に大きくなりすぎないか、根元が地面から浮いたり沈んだりしないか
3. 坂や丘で不自然に地形を突き抜けないか
4. 左右眼で種類・大きさ・高さが一致しているか
5. 近くを通過するときの視差が強すぎず、疾走感が得られるか
6. 配置密度が高すぎず、敵車や道路の視認性を損なわないか
7. 赤黒の画面とプレイヤー車・敵車の画風に馴染んでいるか

## 2026-09-05（19回目）: ヤシの木が小さすぎて判別できないとの実機フィードバックを受け、PALM_VISUAL_SCALE=3.0を一律適用

18回目でヤシの木を実機確認してもらったところ、「よくみると街路樹のいくつかはヤシの木になっていた」が「小さくてヤシの木だと判別しづらい」とのフィードバック。既存の道路脇デコレーション（`draw_tree()`の手続き型の木）と同じ距離スケール式をそのまま再利用していたため、遠方はもちろん通常の走行距離でも赤黒の小さな点にしか見えず、既存の木と見分けがつかなかったことが原因と判明。

### 対応：一律3倍の視覚拡大係数を追加

`PALM_VISUAL_SCALE = 3.0`を新設し、`_draw_palm()`の`target_h_px`（遠近投影で求めた幅・高さ）に一律で掛けるようにした。掛け算の位置は既存の`PALM_MAX_HEIGHT_VIEWPORT_FRAC`（片眼viewportの80%クランプ）・`PALM_NEAR_CULL_MARGIN`（near clipでの即カリング）チェックより**前**にしたため、両安全装置とも「3倍後の最終サイズ」に対してそのまま機能する。根元中央アンカーの計算式（`sx - anchor_x * sprite_scale`）はスケール非依存の構造のため、3倍後も根元は地面に固定されたまま浮かない。既存街路樹・敵車・プレイヤー車・ワールド座標・道路からのオフセット・立体視/視差計算・坂道追従・丘の遮蔽・nearest-neighbor拡縮はすべて無変更。

`pytest`は275件、全て成功。`python main.py --race --test-frames 180`・`config.json`無変更も確認。通常走行時2枚・接近時1枚・超接近時（near clipでのカリング確認）1枚の確認画像をユーザーへ送付。**この時点ではユーザーからの実機確認結果が出るまでコミット・pushを保留した。**

## 2026-09-05（20回目）: 遠方で3倍は不自然との実機フィードバックを受け、距離別スケールカーブへ再設計、実機確認OK

19回目の一律3倍を実機確認してもらったところ、「近くを通過するときの3倍サイズは自然だったが、遠方では大きすぎて不自然だった」とのフィードバック。距離によらず一律倍率をかけていたことが原因のため、距離に応じて倍率を変える設計への変更を指示された。

### 対応：ENEMY_SPRITE_TARGET_RATIO_POINTSと同じパターンの距離別スケールカーブ

一律`PALM_VISUAL_SCALE`を廃止し、`PALM_SCALE_POINTS`（`(tz, 倍率)`の制御点、tz昇順・倍率降順）と`_palm_visual_scale(tz)`（制御点間をsmoothstep補間、両端でフラットに保持）を新設した。既存の敵車サイズカーブ（`ENEMY_SPRITE_TARGET_RATIO_POINTS`/`_enemy_target_ratio`）と全く同じ実装パターンの再利用。ユーザー指定の初期値をそのまま採用：

```
PALM_SCALE_POINTS = (
    (3.0, 3.0),    # 最近接（project()のtrans_zの下限＝理論上の最近接点）
    (15.0, 2.6),
    (30.0, 2.0),
    (60.0, 1.5),
    (90.0, 1.25),
)
```

敵車の`ENEMY_SPRITE_TARGET_RATIO_POINTS`のドキュメントコメントで指摘されている「単調に減少する2つの関数（生の遠近スケールと倍率カーブ）の積は、両方とも同じ向き（距離が縮むほど増加）に単調であれば、積も単調非減少になる」という数学的性質を確認した上で採用（敵車で単調性が壊れたのは、`boost`が逆方向＝遠いほど増加する設計だったため）。念のため、(1) `PALM_SCALE_POINTS`単体の密なサンプリング（tz=0〜125を0.25刻み）で単調性・両端でのフラット・最大値3.0を超えないことを直接検証するテスト、(2) 既存の`target_h_px`密サンプリングテストを倍率込みの実際の式へ更新、の2つで単調性を担保した。

適用位置・安全装置（最大高さクランプ・near-cullマージン）・根元アンカー・既存街路樹/敵車/プレイヤー車/ワールド座標/立体視/坂道追従/丘の遮蔽は19回目と同様すべて無変更。

`pytest`は275件→**276件**に増加、全て成功。`python main.py --race --test-frames 180`・`config.json`無変更も確認。90m（1.25倍）・60m（1.5倍）・30m（2.0倍）・15m（2.6倍）・実際に描画される最接近点（理論上の最近接tz=3.0はnear clipで実際にはカリングされ表示されないため、代わりに描画される最も近い点＝約10m、倍率約2.75倍）の5枚の比較画像をユーザーへ送付し、確認を得た。

### 実機確認結果：OK、Maker Faire展示に最低限使える状態と判断

ユーザーから「実機確認が完了し、最低限展示できる状態になりました」との報告。今回のセッションはここで区切りとし、現在の実装内容を正式版として確定・コミット・pushする（本エントリ）。

## 現在のステータス

動く疑似3Dレーシングゲーム本体の実装が完了。カーブ形状・高速コーナリング・3車線化・カーブの緩和（2段階）・カーブ中の背景パン/車のリーン・カーブ中のカクつき修正・スピード感向上（240→320km/h→実質360km/h相当を320表示）・道路の起伏（上り坂・頂上・下り坂・谷、頂上での遮蔽込み）・坂の強調・早期化とカーブの「うねうねS字」化・BGM選択画面・エンジン音/タイヤ軋み音のSEに加え、SEの音量修正（2026-09-04）・BGMの初期音量設定（2026-09-04）・Maker Faire展示用RESET機能（2026-09-04、実機確認OK）・SE unavailable診断機能とミキサーpre_init（2026-09-04、実機で有効性を確認）・エンジン音のRPMモデル/波形全面再設計＋3プリセット（2026-09-04、実機評価済み、ARCADE ENGINE・`volume=0.4`で確定）・タイヤ軋み音の発火頻度引き上げ＋3プリセット化＋音量微調整（2026-09-04〜05、実機評価済み、CLASSIC SQUEAL・`volume=0.6`で確定）・Xboxコントローラー正式対応（2026-09-05、`gamepad.py`新設、診断ログ・SELECT MUSIC操作・レース中操作・デッドゾーン・抜き差し対応込み、D-pad/スティック上下でのアクセル・ブレーキ込み、実機で「いいですね」と確認済み）・プレイヤー車の5方向ピクセルアート化（2026-09-05、`assets/cars/player/`新設）・プレイヤー車を本番用スプライトシート（外部作成PNG、88×48px）へ差し替え（2026-09-05、`assets/cars/player/build_from_spritesheet.py`新設）・敵車を6種の本番スプライトへ差し替え（2026-09-05、`assets/cars/enemies/`・`scripts/build_enemy_sprites.py`新設、車種抽選は既存レーン用rngと独立した専用rng）・実機確認で判明した敵車接近時のサイズ異常（panel_van巨大化）を修正し、遠方視認性とのバランスを取った非線形サイズカーブへ再設計（2026-09-05、`ENEMY_SPRITE_TARGET_RATIO_POINTS`）・実機A/B比較（`F7`一時ツール）を経て敵車サイズを最終確定（2026-09-05、同深度で約68%、90/60/30mも同倍率で調整、`F7`ツールは削除済み）・道路脇へのヤシの木6種の配置（2026-09-05、`assets/scenery/palms/`・`scripts/build_palm_sprites.py`新設、既存の道路脇デコレーション/敵車/コース生成のrng・配置・立体視・坂道遮蔽ロジックはすべて再利用のみで無変更）・ヤシの木の表示サイズを実機A/B相当のフィードバック2回を経て距離別スケールカーブへ確定（2026-09-05、`PALM_SCALE_POINTS`/`_palm_visual_scale`、実機確認OK）まで実装済み。RESET機能・エンジン音・タイヤ軋み音・Xboxコントローラーの基本操作、敵車サイズ、ヤシの木の表示サイズは実機確認済み、D-pad/スティック上下でのアクセル/ブレーキ・タイヤ音量調整・プレイヤー車と敵車の本番グラフィック（最終サイズ含む恒久反映版）は実機での確認待ち。Maker Faire展示用N150ミニPCへの本番環境移行がまだ（下記`docs/NEXT_STEPS.md`参照）。
